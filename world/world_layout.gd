class_name WorldLayout
extends RefCounted

## 世界布局的**共享常量** —— "岛周边环"的单一事实来源。
##
## 【为什么单独一个文件】草原生成器与湖泊生成器都要表达"地貌中心甩到离地图正中多远"。
##   如果各自写一个 50，那叫**同值复制** —— 改一处忘一处，两者立刻错位，
##   而且谁都不知道对方也在用这个数（隐式耦合）。
##   收敛到这里之后：两个生成器都只认识这一个数，**彼此互不认识**（横向解耦）。
##
## 【为什么不是 .tres / LakeDef 字段】"整个世界的尺寸基准"不是某一种地貌的私有参数，
##   是它们共同的前提。放进某一种地貌的定义里，等于让另一种反向依赖它。

const RING_RADIUS := 50.0   # 地貌中心离地图正中的距离（格）
