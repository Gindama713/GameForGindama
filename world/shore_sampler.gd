class_name ShoreSampler
extends RefCounted

## 「岸线采样器」协议 —— 任何"能造出一片水"的东西都实现它；
## 岸线渲染层（`WaterLayer`）**只认这个类**，不认任何具体生成器。
##
## 【为什么要有这层协议】渲染层要画一条贴着水边的线，就必须能问出
##   "这个点在水里吗 / 离岸多远"。如果它直接去抓某个生成器的静态变量（旧版就是
##   `LakeGenerator.last_field`），那就与那个生成器**硬绑死**了 ——
##   换生成器、加一种水体、让一块水来自别的来源，全都得回来改渲染层。
##
## 有了这层协议，两边各干各的、互不认识：
##   - **生成器**（如 `LakeGenerator` 内部的 `LakeSampler`）只管产数据；
##   - **渲染层**只管把数据画成线。
##   这就是"降低耦合"的落点：依赖指向**抽象**，不指向**实现**。
##
## 【为什么协议里要有 bounds()】渲染层必须知道"烘多大一张贴图"。
##   让采样器自己报包围盒，渲染层就**不需要去扫地形网格** ——
##   于是它对 `GridManager` / `Terrain` 的依赖也一并消失。
##
## 【坐标约定】全部是**格坐标**：格 (x,y) 的格心 = (x + 0.5, y + 0.5)。
##   换算成像素（× `Grid.CELL_SIZE`）是渲染层的事，采样器不关心。

## 归一化有符号值：< 0 = 水内，> 0 = 陆，== 0 = 正好在岸线上。**子类必须覆盖**。
func e(_wx: float, _wy: float) -> float:
	return 1.0

## 这片水的包围盒（**格坐标**，不含像素换算）。**子类必须覆盖**。
func bounds() -> Rect2:
	return Rect2()
