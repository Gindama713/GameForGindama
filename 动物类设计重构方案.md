# 动物类设计重构方案（解耦 / 职责划分）

> 触发：用户指出「家的位置」不该进基类——只有部分动物需要。
> 目标：把这条原则推广到整个 Creature 类体系，划清 **基类 / 组合 / 数据** 三条线。
> 本文**取代**《生物模拟系统设计.md》第 2 节的节点结构，以及《生物大类基础属性.md》里任何"固定字段"的表述。
> 数值仍全标 `待定`。

---

## 1. 原则（从「家的位置」提炼）

> **基类里，每一样东西都必须对 100% 的生物都成立。**
> 只要存在一只生物不需要它，它就不属于基类——要么做成**可选组件**，要么放进**数据**。

「家的位置」失败在哪：鱼、游荡的食草动物没有家。把它写进基类，等于让每条鱼都背着一个无意义字段，而且**每加一个"只有部分物种要"的属性，基类就胖一圈**，最后变成 god class。

---

## 2. 判定三问 + 三类归属

对每个属性/方法问三句：

1. **是不是每个生物都无条件需要它？** —— 是 → 基类；否 → 往下。
2. 如果不是，它是**一种能力/需求**（会持续做事、有状态），还是**一堆数值**？ —— 能力/需求 → 组合组件；数值 → 数据。
3. 它是不是**只在某一类物种成立、且行为差异极大**？ —— 可以考虑一层**浅子类**，但**优先组合**（见第 5 节）。

三条归属线：

| 归属 | 机制 | 装什么 | 例子 |
|---|---|---|---|
| **基类字段** | `Creature` 上的成员 | 100% 通用状态 | 坐标、生死、血量、组件表 |
| **组合组件** | 按需装配的能力/需求对象 | "有时候需要"的东西 | 家的位置、饥饿、部位、背包 |
| **数据（Resource）** | `CreatureDef` / `*Def` | 数值、配置、清单 | 衰减率、部位列表、挂哪些组件 |

> **继承**只用于"稳定的宽泛原型"，且必须浅。默认走组合。

---

## 3. 现有设计的审计表（逐条）

| 项 | 现状位置 | 应归属 | 机制 | 理由 |
|---|---|---|---|---|
| 网格坐标 `coord` | 基类 | **基类** | 字段 | 每个生物都在世界里 |
| `alive` / `health` | Metabolism | **基类** | 字段 | 每个生物都能被伤害/死亡 |
| 状态效果容器 | Body | **基类** | 容器 | 容器通用；装什么(出血/中毒…)靠数据 |
| 组件表 `get_component()` | 无 | **基类** | 字段 | 组合的前提 API |
| 饥饿 `hunger` | Metabolism 固定 | `HungerNeed` | 组合 | 绝大多数动物要，但仍按数据挂 |
| 口渴 `thirst` | Metabolism 固定 | `ThirstNeed` | 组合 | **鱼不需要** |
| 体温 `body_temp` | Metabolism 固定 | `WarmthNeed` | 组合 | 冷血/恒温差异大 |
| 疲劳 `fatigue` | Metabolism 固定 | `FatigueNeed` | 组合 | 不休息的生物不需要 |
| 呼吸 `oxygen` | 未列 | `BreathNeed` | 组合 | **陆生不需要憋气** |
| 意识/击晕 | 未列 | `Consciousness` | 组合(默认挂) | 有神经的才晕；植物/无神经不挂 |
| 部位化身体 `Body/Part` | 基类固定 6 部位 | `PartedBody` | 组合 | **部位清单物种不同；蛇无四肢** |
| 总血量取法 | Metabolism | 血在基类；聚合逻辑在 `PartedBody` | — | 血是通用量，部位是可选细节 |
| 能力属性 `Vitals`(力量/敏/体) | 基类固定 | `Stats` | 组合/数据 | **不是所有生物都有这三项** |
| 背包 `Inventory` | 基类固定节点 | `InventoryComponent` | 组合 | 只有会用容器的物种有 |
| 技能/配方 `Skills` | 基类固定(仅玩家) | `CraftingComponent` | 组合 | 只有会制作的物种有 |
| **家的位置 `home`** | AI 里隐含(DenFinder) | `HomeComponent` | 组合 | **只有筑巢/领地/回窝的才需要** |
| 大脑 `Brain` | 基类固定(仅NPC) | 基类"槽" + AI 模块 | 组合 | 槽通用，模块可选 |
| AI 模块(Prey/Threat/Friend/Item/Den/Discomfort) | 列出 | 可选模块 | 组合 | 每模块按物种挂 |
| 攻击 `combat` | 未列 | `CombatComponent` | 组合 | 攻击方式差异大；和平物种无 |
| 繁殖/年龄/性别 | 未列 | `ReproductiveComponent` | 组合 | 只有会繁殖的物种 |
| 可驯化 | 未列 | `TameComponent` | 组合 | 只有可驯化物种 |
| 阵营/关系表 `faction` | 未列 | 容器在基类 / 内容在数据 | 数据 | 所有生物对"他者"都有态度 |
| `Sprite` 表现 | 子节点 | 表现层组件 | 组合 | 纯表现，可无 |
| `eat()` `drink()` `sleep()` | 基类方法(隐含) | 各自 Need 组件 | 组合 | 行为跟着需求走 |
| `craft()` | Skills | `CraftingComponent` | 组合 | 同上 |
| `pick_up()` / `drop()` | Inventory | `InventoryComponent` | 组合 | 同上 |
| `go_home()` / `find_den()` | Brain | `HomeComponent` + Homing 模块 | 组合 | 依赖家的位置 |
| `take_damage()` / `die()` / `heal()` | 基类 | **基类** | 方法 | 100% 通用 |
| `reproduce()` | 未列 | `ReproductiveComponent` | 组合 | 只有会繁殖的 |

---

## 4. 重点示例：「家的位置」怎么改

### ❌ 错（写进基类）
```gdscript
class_name Creature
var home_position: Vector2i   # 每条鱼、每只游荡的鹿都背着它
var has_home := false         # 又一个只为部分物种存在的字段
```
后果：基类随"部分物种特性"不断膨胀；鱼身上有个永远没用的 `home_position`；判断"这生物有没有家"要靠布尔开关满天飞。

### ✅ 对（组合组件）
```gdscript
# components/home_component.gd
class_name HomeComponent
extends RefCounted
var home_coord: Vector2i
var radius: int = 0          # 0 = 只是一个巢点；>0 = 领地半径
var established := false     # 是否已确立

func is_at_home(c: Vector2i) -> bool:
    return established and c.distance_to(home_coord) <= radius
```
- 狼的 `CreatureDef`：`components = ["HungerNeed", "ThirstNeed", "HomeComponent", ...]`
- 鱼的 `CreatureDef`：**不列** `HomeComponent` → 鱼身上根本没这个概念
- AI 模块 `HomingBehavior` 声明 `requires = ["HomeComponent"]`；装配时校验，缺依赖就拒绝挂载/报配置错。

**这条模式 = 你要的「灵活耦合」**：需要它的物种自己引入，耦合关系是**显式声明**的，不是焊死在基类里。

---

## 5. 为什么优先组合，而不是继承

继承的经典翻车案例（用动物打比方）：
- 「会飞的是鸟」→ 企鹅是鸟但不会飞。
- 「会游的是鱼」→ 鲸会游但是哺乳动物。
- 「飞鱼」→ 既像鸟又像鱼，继承树直接裂开。

**组合不会裂**：飞鱼 = `Creature` + `SwimComponent` + `GlideComponent`，两个能力各挂各的，无需给"飞鱼"新开一个类。

结论：**物种差异靠"挂哪些组件 + 配什么数据"，不靠继承树**。真要继承，只留一层稳定原型（如 `Creature` → `Animal`），且子类里也不能塞"部分物种才要"的东西。

---

## 6. 重构后的骨架

### 6.1 基类底线（只放这些，再多就是耦合）
```
Creature
├─ species_def: CreatureDef      # 我是谁（数据引用）
├─ coord: Vector2i               # 我在哪格
├─ alive: bool / health: float   # 我的命（通用）
├─ _components: Dictionary       # 组件表
├─ _status_effects: Array        # 状态效果容器（内容靠数据）
├─ get_component(cls) / has_component(cls) / add_component(c)
├─ tick(delta)                   # 唯一推进入口：遍历组件 tick
├─ 信号 hurt / died（经 EventBus）
└─ save() / load() 钩子（委托给组件）
```

### 6.2 组件清单（按需装配，全部可选）
- **需求类**：`HungerNeed` `ThirstNeed` `WarmthNeed` `FatigueNeed` `BreathNeed`
- **身体类**：`PartedBody`（部位+聚合血量）、`Consciousness`（意识/击晕）
- **能力类**：`InventoryComponent` `CraftingComponent` `CombatComponent` `Stats`
- **社会类**：`HomeComponent` `ReproductiveComponent` `TameComponent`
- **AI 类**：`Brain` 槽 + 模块 `PreyTracker` `ThreatTracker` `FriendTracker` `ItemTracker` `HomingBehavior` `DiscomfortTracker`

### 6.3 数据（Resource）
```
CreatureDef (Resource)
├─ components: Array[StringName]     # 本物种激活哪些组件（声明式组合）
├─ 通用数值：health_max、mass…
├─ 各组件参数（子 Resource）：
│    HungerNeedDef { max, rate }
│    HomeComponentDef { radius }
│    PartedBodyDef  { parts: ["head","torso","arm_l","arm_r","leg_l","leg_r"] }
└─ diet / 行为脚本路径（NPC用）
```

---

## 7. Godot 落地机制（怎么"挂"组件）

| 方案 | 适用 | 取舍 |
|---|---|---|
| **RefCounted 组件 + 注册表**（推荐） | 上千个模拟实体 | 轻量、无树开销、`get_component` 直接字典查；缺点是没自动 `_process`，由 `Creature.tick()` 统一驱动 |
| 子 Node 组件 | 需要独立生命周期/信号/可视化的少数组件 | Godot 原生、编辑器可见；缺点是有树开销，几千个实体时明显 |

依据：之前核实的引擎内存模型——`Node` 在树里、有遍历开销；`RefCounted` 引用计数、轻。**模拟对象多 → 选 RefCounted**。
`get_component()` 这套 API 把两种实现的差异藏起来，将来某个组件想升级成 Node 也不改调用方。

---

## 8. 落地步骤（小步）

1. 定基类底线 + 组件协议 `tick(delta)` / `save()` / `load()` / `def`。
2. 定组件注册表 + `get/has/add_component()`。
3. 只实现 `HungerNeed` 一个组件（最小验证：print 看饥饿随时间掉）。
4. `CreatureDef` 改成**声明式**（`components` 列表），猪的 def 挂 `HungerNeed`，把"配数据即得新物种"跑通。
5. 再加 `PartedBody`（部位→能力）与 `HomeComponent`（狼有巢、鱼没有）。
6. 最后接 AI 模块 + 依赖校验（`HomingBehavior` 缺 `HomeComponent` 就报错）。

---

## 9. 待你拍板

1. **组件实现**：RefCounted 注册表（我推荐，可上千实体）还是子 Node（编辑器直观）？
2. **意识/击晕**：放基类（所有动物都能晕）还是做成组件（将来加植物/真菌就省事）？
3. **要不要一层浅继承**（`Creature` → `Animal`），还是**纯组合**？我倾向纯组合。
4. **`Stats`(力量/敏捷/体质)**：保留为组件，还是干脆并进数据、不做独立组件？
