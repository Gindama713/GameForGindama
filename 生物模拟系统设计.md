# 生物模拟子系统设计（Plan）

> ⚠️ 本文第 2 节的节点结构（Metabolism/Body/Vitals/Inventory/Skills 全作固定子节点）已被 **《动物类设计重构方案.md》** 取代。
> 重构后：基类只留 100% 通用的状态；需求/部位/背包/技能/家的位置全部改为**按需装配的组件**，由 `CreatureDef` 声明式引入。以那份为准，本文其余部分（时钟、信号、落地顺序）仍有效。

> 范围：只讲「生物（玩家 / 猪 / 狼…）的底层逻辑怎么统一」。不铺全游戏。
> 原则：所有生物共用一套代码，差异只在**数据（CreatureDef）**与**大脑（Brain）**。
> 数值全部标 `待定` —— 真值要在游戏里跑出来调，不预先编造。

## 1. 核心原则
- **统一 Creature 基类**：玩家、猪、狼都是同一个 `Creature` 节点树 + 同一套子系统脚本。
- 区别不是「另写一套代码」，而是：
  - 喂不同的 `CreatureDef`（物种数据：有哪些身体部位、基础属性区间、吃什么、会什么）；
  - 挂不同的 `Brain`（玩家=输入，NPC=AI 脚本）。
- 这保证你后面加「螃蟹 / 鸟 / 丧尸」都是**配数据**，不写新系统。

## 2. 节点结构（细化前面那棵节点树）
```
Creature (Node2D / Node)          # 生物根：位置=所在格；自身不写逻辑
├─ Metabolism (Node)              # 实时属性：饥饿/口渴/疲劳/体温 + 总血量
├─ Body (Node)                    # 身体部位容器
│   ├─ Part: head
│   ├─ Part: torso
│   ├─ Part: arm_left / arm_right
│   └─ Part: leg_left / leg_right
├─ Vitals (Node)                  # 能力属性：力量/敏捷/体质…（影响能做啥）
├─ Inventory (Node)               # 携带物品（ItemDef 引用）
├─ Skills (Node)                  # 仅玩家：技能等级 + 已解锁配方集合
├─ Brain (Node)                   # 仅 NPC：AI；玩家的"脑"是输入，不挂此节点
└─ Sprite (Sprite2D)              # 可选：一个 game-icons 图标；纯文字风可删
```

## 3. 实时属性（Metabolism）
- 每个属性是 `当前值 / 上限` 的一对浮点，存实例里（当前状态）。
  - 饥饿 hunger、口渴 thirst、疲劳 fatigue、体温 body_temp、总血量 health（待定清单）
- **定义（衰减率、上限、阈值）来自 `CreatureDef` 的 Resource**，不硬编码。
- **推进方式**：`Metabolism._physics_process(delta)` 用 `TimeSystem` 的时间增量推进：
  - `hunger -= def.hunger_rate * dt`
  - 到阈值（如饥饿=0）发信号 `EventBus.hunger_depleted` → 掉血 / 限制行动。
- 用 `_physics_process` = 固定步长、确定性、**暂停即停、可快进**（对模拟游戏关键）。

## 4. 身体部位（Body / Part）
- 每个部位是一个对象（参考前面 `Grid.Cell` 的做法：`Part` 类 + 容器），自带属性：
  - `hp`（部位血量）、`infected`（感染）、`bleeding`（出血）、`function_ok`（功能是否正常）
- 部位状态**直接决定能力**（涌现式，不是查表）：
  - 腿 `function_ok=false` → 移动力降为 0（不能走到别的格）
  - 手臂 `function_ok=false` → `can_craft=false`（做不了东西——呼应你"做坏过才学得会"的调性）
  - 头部 `hp=0` → 生物死亡
- 总血量 `health` 两种取法（待定选一）：
  - (a) 各部位 `hp` 之和聚合；或 (b) 单独一条 `health`，部位伤按权重扣它。
  - 倾向 (a)：更细、更 CDDA，但 UI/平衡更麻烦。

## 5. 数据驱动（Resource 层）
- `CreatureDef`（物种数据，全局共享，引用计数）：
  - `body_parts: Array[StringName]`（如 `["head","torso","arm_left",...]`）
  - 各属性的 `max` 与 `rate`（衰减/回复）
  - `diet`（吃什么）、`behavior`（AI 脚本路径，NPC 用）
- 生物实例**只存当前状态**（如 `hunger=12.3`）。定义改数据文件即可，不碰代码。
- 物品 `ItemDef`、配方 `RecipeDef`、技能 `SkillDef` 同样是 Resource（前面已规划）。

## 6. 时钟与确定性
- `TimeSystem`（Autoload）是唯一时钟；所有实时属性向它要 `delta`，不自己读系统时钟。
- 这样：暂停 = 所有 `_physics_process` 停；快进 = 给 `TimeSystem` 喂更大 `delta` 或多次 tick。

## 7. 玩家 vs NPC：统一与差异
- **同一套 `Creature` 代码**，二者都满足上面结构。
- 差异：
  - 玩家：`Skills` 节点有数据；无 `Brain`（决策来自玩家输入 + UI）。
  - NPC：`Brain` 挂 AI 脚本（觅食/逃跑/攻击），`Skills` 通常空或简化。
  - 狼吃猪、猪吃草——都是 `diet` + `Brain` 数据区别，代码不分叉。

## 8. 信号 / 解耦（呼应前面架构）
- `Metabolism` / `Body` 只发信号（`hungry` / `hurt` / `died`），不持有 UI 或威胁系统的引用。
- `EventBus`（Autoload）转发 → UI 显示、威胁系统反应。对象释放后连接自动断（引擎机制）。

## 9. 落地顺序（小步，先逻辑后表现）
1. `Creature` 根 + `Metabolism` + `Body/Part` 纯逻辑，**用 print 看数值随时间变化**（不画任何东西）。
2. 接 `TimeSystem` 时钟，确认暂停/快进正确。
3. 部位状态→能力（断腿不能动），先逻辑断言后表现。
4. 一个 `CreatureDef`（比如猪）+ 在 `(0,0)` 放一只，打印它的饥饿在掉。
5. 之后才做玩家大脑 / UI / 图标显形。

## 10. 待你拍板（不替你定）
- 总血量取 (a) 部位聚合 还是 (b) 单独血条？
- 身体部位清单：先 6 格（头/躯干/双臂/双腿）够不够，还是要有手/脚/躯干分段？
- 属性衰减率初值：先给一组明显能感知的占位值（如 1 分钟饿到 0），调手感再改。
