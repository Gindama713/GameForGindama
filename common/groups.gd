class_name Groups
extends RefCounted

## 组标（Godot 原生 `add_to_group()` / `is_in_group()` 用的字符串）的**唯一事实来源**。
##
## 【为什么单独一个类】这些字符串此前**散在三处各写一遍**，而且每处的注释都写着
## "与 XXX 同一个字面量" —— 那正是"同值复制"的自我供认：改一处忘一处，
## 而且没人知道还有谁在用这个数（隐式耦合）。
##   · `main.gd`            —— 生成主角时 `add_to_group(GROUP_PLAYER)`
##   · `perception.gd`      —— `threats()` 按它筛威胁
##   · `ui/creature_inspector.gd` —— 靠它避开主角（主角有自己的档案面板）
##
## 【为什么不放到 `Creature` 上】组标不是生物自身的属性 ——
##   "谁在扮演玩家"是**编排层的知识**（换了控制方式它还是同一条命），
##   往 `Creature` 基类塞 `GROUP_PLAYER` 等于让基类认识"玩家"这个概念，
##   违反本项目「主角严格是一个物种、基类不认识任何业务身份」的纪律。
##   放在这个中立的常量类里，三方都只依赖它、彼此互不认识。
##
## ⚠ 加新组标时**只在这里加**。判断某个组标该不该进这里：
##   "是不是有 ≥2 个互不相识的文件都在写同一个字符串" —— 是就进来。

## 主角身上的组标。全场唯一，由 `main.gd` 在生成主角时打上。
const PLAYER := "player"
