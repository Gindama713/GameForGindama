# 事实文档

> **本文件是本项目唯一的事实基准（single source of truth）。**
>
> 建立日期：2026-09-18 · 对应代码状态：可 headless 跑通（Godot 4.7.2 stable steam）
>
> ---
>
> **权威性规则**
> 1. 本文件的每一条陈述都必须能在代码里找到对应物。**描述的是「现在是什么」，不是「希望是什么」。**
> 2. 与本文件冲突的其它文档，一律以本文件为准（见 §9 文档状态表）。
> 3. 「打算做但还没做」的东西**不许**混进「已实现」区，必须写在 §8。宁可写「未决」，不许编。
> 4. 代码改了，本文件同步改。改代码不改文档 = 文档作废。
>
> **标注约定**（全文统一）
> - `[已实现]` 代码存在且实测跑通
> - `[已拍板未实现]` 用户已明确决定要，但代码还没有
> - `[未决]` 还没定，不许当既定事实用
> - `[休眠]` 代码存在但无人调用 / 无接线（不是功能，是预留）
> - `[已知问题]` 确认存在的缺陷或坑
> - `[占位]` 数值未调，只为跑通流程，必然要改

---

## 一、项目目标与范围

### 1.1 一句话

一个 **2D 网格荒岛求生模拟游戏**：玩家落魄荒岛，靠采集 / 制作 / 建造 / 应对动植物活下来。
系统深度优先于画面表现——目标是「每个动作都有后果、世界会自己运转、故事由系统叠加涌现」，而不是剧本。

> 说明：以上为项目**长期目标**。就当前代码而言，**玩家与荒岛都还不存在**（见 §1.3）。

### 1.2 设计支柱（决定一切取舍的根）

| 支柱 | 含义 | 对架构的强制要求 |
|---|---|---|
| 模拟深度优先 | 不追求画面，追求因果链完整 | 逻辑层与表现层可分离；表现删掉不影响模拟 |
| 涌现式叙事 | 不写剧本，靠系统叠加出故事 | 系统之间不写死交互，靠状态组合 |
| 数据驱动内容 | 物品/配方/生物/地块用数据定义 | 加内容不改代码 |
| 后果与永久死亡 | 错误有代价，死亡有终结 | 生死必须是单一事实来源，不可歧义 |
| 可读性 | 图标 + 文字 + 事件日志 | 世界状态要能被「读懂」，不靠美术暗示 |

### 1.3 范围

**在范围内（最终要做）**：时间与昼夜、岛屿地形、玩家属性与代谢、分部位伤害与伤病、背包（重量+体积）、制作与技能、天气与温度、火、动植物生态、庇护所建造、水源与净化、食物保存、探索与地图记忆、存档。

**当前代码实际拥有的**（截至本文档日期）：
- 一个逻辑网格（40×24）+ **DF 式网格渲染**（黑=陆地、极暗格线）+ **右上角小地图**（缩略全图，生物=色块）
- 一套窗口自适应（画面随窗口等比放大）
- 一个全局时钟（可暂停/倍速的接口，但无控制界面）
- 一只会自己随机走动的**猪**（唯一存在的生物，画面上是 `def.map_color` 色块，默认白）
- 它的身体部位 / 需求 / 出血 / 死亡（要害失能、饥渴冻致死两条死法）
- 一个调试工具栏 + 一个生物检视面板（含活体/尸体两种视图）
- 一套 EventBus 信号与日志出口
- **一套启动期定义校验**（配数据错了会当场报错，而不是运行期静默出事）

**明确还没做**（不许当已有）：
- **玩家角色**（没有 avatar，没有输入控制，没有 Camera2D 跟随）
- 地形数据与地图生成（terrain 目前全是 `&"unknown"`，色表只有黑一项）、TileMap
- 物品、背包、制作、技能、战斗、驯化
- 天气、温度场、水源、火
- 存档 / 读档
- 除猪以外的任何生物

> 记忆点：**现在的项目是一个「生物模拟内核 + 调试壳」，不是一款能玩的游戏。**

---

## 二、技术基线

### 2.1 引擎与工程设置

| 项 | 实际值 | 备注 |
|---|---|---|
| 引擎 | **Godot 4.7.2.stable.steam** (`ed1daf0bf`) | 早期文档写的「4.6」已过期 |
| 可执行文件 | `/e/SteamLibrary/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe` | E 盘 Steam 库 |
| 主语言 | GDScript（全部静态类型标注） | 性能热点将来再考虑 C# / GDExtension |
| 渲染器 | **Forward+** (`config/features = ["4.7", "Forward Plus"]`) | 早期文档建议的「Compatibility」未采用 |
| Windows 驱动 | `rendering_device/driver.windows = "d3d12"` | |
| 清屏色 | `default_clear_color = Color(0,0,0,1)` **纯黑** | 画面基调：黑底 |
| 主场景 | `res://main.tscn` | |
| 视口基准 | `viewport_width=1280`、`viewport_height=768` = 40×24 格 × 32px，**正好一屏装下逻辑网格** | |
| 窗口拉伸 | `stretch/mode="canvas_items"` + `aspect="keep"` + `scale_mode="fractional"` → **画面随窗口等比放大**、不变形、多出区域留黑（背景本就是纯黑，看不出黑边） | 2026-09-18 由 `aspect="expand"` 改来 |
| 初始窗口 | `window_width_override=1280`、`window_height_override=768`（与视口 1:1，最清晰）；玩家拉大即自动放大 | `resizable` 默认开 |
| 全局主题 | `gui/theme/custom = "res://ui/game_theme.tres"` | |
| 3D 物理引擎 | `Jolt Physics` | 本项目是 2D，此项无实际作用（仅为默认残留） |

**窗口自适应（2026-09-18 实测，非推测）**

判定方法：冻结时钟 + 打开检视面板 + 截图，扫非黑像素包围盒（内容边界），换窗口尺寸比对。

| 窗口 | 渲染区域 | 内容包围盒 | 结论 |
|---|---|---|---|
| 1280×768 | 1280×768 | (8,8)-(668,408) | 基准 1x |
| 1920×1152 | 1920×1152 | (12,12)-(1004,612) | **×1.5 等比放大**（三个坐标都精确 ×1.5） |
| 1600×900（16:9，比例不同） | 1500×900 | (12,12)-(784,476) | 取 `min(1.25, 1.172)=1.172` 等比缩放，左右各 50px 黑边，**不变形** |

- 改前是 `aspect="expand"`：窗口变大只会**多露出空白**，画面不放大 —— 与"随窗口放大"的诉求相反。
- `scale_mode="integer"` 实测：1920×1152 下渲染区域仍是 **1280×768（1x，不放大）**，因为装不下 2x（2560×1536）。所以整数倍模式虽然像素最锐利，但**拉大窗口没反应**；要"随窗口放大"必须用 `fractional`（代价：非整数倍时像素略有不匀）。

### 2.2 目录结构（实际，非规划）

```
新建游戏项目/
├─ project.godot
├─ main.gd / main.tscn            # 主场景：生成猪 + UI 容器
├─ grid.gd                        # Grid + Cell（纯逻辑，RefCounted）
├─ grid_manager.gd                # Autoload: GridManager
├─ time_system.gd                 # Autoload: TimeSystem
├─ event_bus.gd                   # Autoload: EventBus
├─ log.gd                         # Autoload: Log
├─ def_validator.gd               # Autoload: DefValidator（启动期定义校验）
├─ render/
│  └─ map_renderer.gd             # DF 式网格渲染（黑=陆地，色表数据驱动）
├─ creature/
│  ├─ creature.gd                 # 生物基类
│  ├─ creature_def.gd             # 物种数据（Resource）
│  ├─ body_part_def.gd            # 部位数据（Resource）
│  ├─ body_part.gd                # 部位运行时状态（RefCounted）
│  ├─ need_def.gd                 # 需求数据（Resource）
│  ├─ need.gd                     # 需求运行时状态（RefCounted）
│  ├─ pig.tscn                    # 猪的场景（根 + Sprite2D）
│  ├─ pig_def.tres                # 猪的物种数据
│  ├─ components/
│  │  ├─ creature_component.gd    # 组件基类（协议）
│  │  ├─ grid_mover.gd            # 网格移动能力
│  │  ├─ wander_brain.gd          # 游荡决策
│  │  ├─ body.gd                  # 部位化身体
│  │  └─ needs.gd                 # 需求集合
│  └─ data/
│     ├─ part_head/torso/foreleg_l/foreleg_r/hindleg_l/hindleg_r.tres
│     └─ need_food/water/warmth/fatigue/breath.tres
├─ ui/
│  ├─ debug_toolbar.gd
│  ├─ creature_inspector.gd
│  ├─ minimap.gd                  # 右上角小地图（DF 式缩略图）
│  ├─ pixel_bar.gd                # 像素分段条（自绘 Control）
│  ├─ game_theme.tres             # 全局像素风主题
│  └─ fonts/HYPixel11pxU-2.ttf
└─ pic/
   ├─ pig.png                     # 512×512 剪影；主画面猪的贴图（缩放 0.0625 一格）
   └─ wolf-head.png               # 未使用
```

> **注意**：早期《荒岛求生开发计划.md》画的 `data/ core/ world/ entities/ tools/` 目录树**没有采用**。实际是根目录平铺 + 按子系统分文件夹。以本节为准。

### 2.3 验证方式（改完代码必须跑）

```bash
GODOT="/e/SteamLibrary/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe"

# ① 导入资源 + 注册全局类（能暴露 GDScript 解析错误）
"$GODOT" --headless --path "D:/Desktop/GAME/新建游戏项目" --import

# ② 实跑 N 帧后退出（验证运行期行为/报错）
"$GODOT" --headless --path "D:/Desktop/GAME/新建游戏项目" --quit-after 300
```

- `--quit-after` 计的是**主循环迭代数**，headless 下比真实时间快，300 帧 ≈ 少量物理 tick。
- 唯一的正常噪音：`ERROR: Could not open 'user://' directory` —— headless 环境固有，可忽略。
- **`[已知问题]` 坑**：从 bash 跑 headless 会在工程目录里生成 `Godot/app_userdata/.../logs/`，**跑完要 `rm -rf Godot`**，别提交进工程。

### 2.4 已实测的 Godot 4.7 本地坑

| # | 坑 | 现象 | 绕法 |
|---|---|---|---|
| 1 | `.tres` 里用内联子资源 `type="ClassName"`（ClassName 是 GDScript 全局类） | `Cannot get class 'X'` / `Can't create sub resource of type 'X'`，类已注册进缓存也照样失败 | 改**独立 `.tres` 文件**（顶层 `script_class="X"`），引用处用 `ExtResource` 按路径引用 |
| 2 | 在主场景里 `instance = ExtResource(...)` 实例化「根脚本在 `_ready` 里 `add_child` 子节点」的 UI 场景 | 被 Godot 实例系统整段丢弃（`WARNING: modified from inside an instance, but it has vanished`），节点变 null | 不要实例外部 UI 场景；把 UI 作为**主场景的直接节点**（parent 写好）挂脚本 |
| 3 | 给 `Sprite2D` 赋 `input_pickable` | 在本 4.7 构建下赋值报错 | 不走 picking，用坐标查格反算做点击选取（见 §5.8） |

---

## 三、运行时骨架

### 3.1 Autoload（4 个，声明顺序即初始化顺序）

| 名称 | 脚本 | 职责 | 依赖 |
|---|---|---|---|
| `GridManager` | `res://grid_manager.gd` | 持有全局 `Grid` 实例 | `Grid` |
| `TimeSystem` | `res://time_system.gd` | **唯一时钟**，发 `tick(dt)` | 无 |
| `EventBus` | `res://event_bus.gd` | 全局信号中转 | 无 |
| `Log` | `res://log.gd` | 带时间戳/分类的日志出口 | `TimeSystem`（读 `elapsed`） |
| `DefValidator` | `res://def_validator.gd` | **启动期定义校验**：扫描 `res://creature` 全部 `.tres` 并报错 | 无 |

> 早期文档提到的 `GameManager` / `ContentDB` / `SaveManager` **不存在**。

### 3.2 场景树

```
Main (Node2D, res://main.gd)                        ← run/main_scene
├─ MapRenderer (Node2D, res://render/map_renderer.gd)   ← 把网格画成色块地图（树序在生物之前 → 生物画在上层）
├─ UI (CanvasLayer)
│  ├─ DebugToolbar (Control, res://ui/debug_toolbar.gd)
│  ├─ CreatureInspector (PanelContainer, res://ui/creature_inspector.gd)
│  └─ Minimap (Control, res://ui/minimap.gd)             ← 右上角小地图（锚点 TOP_RIGHT，margin 8）

运行时动态生成（挂在 Main 下）：
Pig (Node2D, res://creature/creature.gd)
└─ Sprite2D                       ← 有 texture 的物种走图片；无 texture 的物种走 _draw 色块兜底
```

### 3.3 时间与节拍

- 时钟源：`TimeSystem._physics_process(delta)`（物理帧，固定步长 → 确定性）。
- 物理 tick 频率**未配置**，用引擎默认。
- 推进公式：`d = delta * speed`；`elapsed += d`；`tick.emit(d)`。
- `paused == true` 时**直接 return，不发 tick** → 所有订阅者自然停止。
- **所有生物都订阅 `TimeSystem.tick`，没有生物自己读 `_process`。** 暂停/倍速因此是免费的。
- `TimeSystem.paused` / `speed` 目前 `[休眠]`：字段存在、逻辑读它，但**没有任何代码或 UI 去设置它**（没有暂停键、没有倍速键）。

---

## 四、数据结构

### 4.1 网格层

**`Grid`**（`class_name Grid extends RefCounted`，`grid.gd`）

| 成员 | 类型 | 说明 |
|---|---|---|
| `CELL_SIZE` | `const int = 32` | 一格边长的像素数 |
| `width` / `height` | `int` | 构造时传入 |
| `cells` | `Dictionary` | key `Vector2i` → value `Cell` |

| 方法 | 返回 | 说明 |
|---|---|---|
| `_init(p_width := 40, p_height := 24)` | — | **构造时预建每一格**，保证每格真实存在可寻址 |
| `in_bounds(coord)` | `bool` | 越界判定 |
| `get_cell(coord)` | `Cell` | 越界返回 null |
| `grid_to_world(coord)` | `Vector2` | **`coord * CELL_SIZE`＝格子左上角**（不是中心！居中要自行 `+ (16,16)`） |
| `world_to_grid(pos)` | `Vector2i` | `(pos / CELL_SIZE).floor()` |
| `has_cell(coord)` | `bool` | `[休眠]` 无调用方 |
| `for_each_cell(callback)` | `void` | `[休眠]` 无调用方 |

**`Grid.Cell`**（内部类，纯数据）

| 字段 | 类型 | 说明 |
|---|---|---|
| `coord` | `Vector2i` | 自身坐标（构造时写入） |
| `terrain` | `StringName = &"unknown"` | **预留**：地形类型（沙/石/草…）。目前无任何代码写入或读取 |
| `content` | 无类型 `= null` | **预留**：格子上的生物/物品引用。目前只被用来存 `Creature` 做占位 |

**`GridManager`**（Autoload，`grid_manager.gd`）

- `var grid: Grid`；`_ready()` 里 `grid = Grid.new(40, 24)`。
- `cell_at(x, y) -> Grid.Cell`（内部走 `get_cell`，**不做边界检查**，越界返回 null）
- `in_bounds(x, y) -> bool`

**网格算术事实**：40×24 格 × 32px = 世界范围 **1280 × 768 px**。

### 4.2 定义层（Resource，全局共享的「是什么」）

**`CreatureDef`**（`creature_def.gd`）— 物种数据

| 字段 | 类型 | 说明 |
|---|---|---|
| `display_name` | `String = "生物"` | 显示名 |
| `texture` | `Texture2D` | 主画面图标（猪 = `pic/pig.png`）；**可为 null** → 主画面用 `map_color` 色块兜底 |
| `map_color` | `Color = 白` | **小地图**上的色点颜色；也是无图物种在主画面的兜底色 |
| `component_scripts` | `Array[Script]` | **声明式组合**：这个物种挂哪些组件 |
| `body_parts` | `Array[BodyPartDef]` | 部位清单；空数组 = 没有身体系统 |
| `active_needs` | `Array[NeedDef]` | 激活的需求；空数组 = 没有需求系统 |

**`CreatureDef.validate() -> Array[String]`**（定义校验）`[已实现]`
返回问题清单，空数组 = 通过。检查项：`display_name` 非空；部位 id 非空/不重复/`max_hp > 0`；**至少一个 `is_vital` 部位**（否则该物种天生不死）；需求 id 非空/不重复/`max_value > 0`/`drain_rate >= 0`；组件脚本有资源路径/不重复挂载/实例化成功/`requires()` 依赖齐全。
两处调用：**启动期**由 `DefValidator` 扫全部定义；**生成期**由 `Creature._ready()`，不通过就拒绝生成。
> 内部拆成 `_validate_parts` / `_validate_needs` / `_validate_components`。组件依赖校验**分两遍**（先收齐路径再查依赖），这样「依赖方写在被依赖方前面」不会误报。
> **不检查**「有没有 stance 肢体」—— 鱼/蛇本来就没有腿，无 stance 是合法配置。

**`BodyPartDef`**（`body_part_def.gd`）— 部位定义

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | `String` | 唯一标识，如 `"head"` |
| `label` | `String = "部位"` | 中文显示名 |
| `max_hp` | `float = 100.0` | 部位血量上限 |
| `is_vital` | `bool = false` | **要害**：归零即判宿主死亡 |
| `limb_types` | `Array[String]` | 该部位承担的肢体职能集合。`"stance"`=支撑/行走，`"grasp"`=抓握。**空 = 不是肢体** |
| `category` | `String = "other"` | **仅显示/分组用，不参与任何规则判定** |

方法：`is_limb()`（`limb_types` 非空即真）、`has_limb_type(t)`。

> 设计要点（依据 CDDA `body_part_type`）：`is_vital` / `limb_types` 是**数据字段**，不是代码里 `category == "head"` 这种字符串比较。加一个要害部位、加一条腿，都只是配数据。

**`NeedDef`**（`need_def.gd`）— 需求定义

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | `String` | 如 `"food"` |
| `label` | `String = "需求"` | 中文显示名 |
| `max_value` | `float = 100.0` | 上限（同时也是初始值） |
| `drain_rate` | `float = 1.0` | **每秒衰减量** `[占位]` |
| `depleted_is_lethal` | `bool = false` | **见底即死境**。由 `Needs.is_lethal()` 上报、`Creature` 统一执行。饥饿/口渴/保暖/憋气 = true；疲劳 = false |

### 4.3 运行时状态层（RefCounted，个体的「现在是多少」）

**`Creature`（生物基类）** — `class_name Creature extends Node2D`，`creature/creature.gd`

| 成员 | 类型 | 说明 |
|---|---|---|
| `def` | `CreatureDef`（`@export`） | 我是谁 |
| `coord` | `Vector2i = Vector2i.ZERO` | 我在哪一格（**逻辑位置**） |
| `_alive` | `bool = true`（**私有**） | 生死唯一事实来源。外部**只读** `is_alive()`，**只有 `die()` 能改它** |
| `id` | `int` | 全局自增编号，日志用 |
| `rng` | `RandomNumberGenerator` | **个体随机源**。按 `世界种子 + 递增序号` 派生，替代全局 `randf/randi` → 存档/回放可复现 |
| `_components` | `Dictionary` | key = 组件脚本的 `resource_path`（String） → value = 组件实例 |
| `BODY_SIZE` | `const 20.0` | 自绘色块边长（格子 32px，留边显"棋子"感） |
| `DEAD_COLOR` | `const Color(0.32,0.32,0.32)` | 死亡后的色块颜色 |

方法清单：
`tag()` / `_log_spawn()` / `_unhandled_input()` / `get_component(script)` / `has_component(script)` / `get_components()` /
`is_alive()` / `is_dead_state()` / `lethal_reason_text()` / `check_dead_state()` / `die()` /
`_on_tick(dt)` / `_build_components()` / `_fail_init(reason)` /
`can_place_at(c)` / `_place_at(c)` / `_release_cell()` / `move_to(target)` / `_draw()`

> **表现**：生物没有 Sprite2D，画面 = `_draw()` 在格子中央画 `def.map_color` 色块（死了变 `DEAD_COLOR`，`die()` 里 `queue_redraw()`）。

> **基类不认识任何具体组件。** 它没有 `get_body()` / `get_needs()` 这类便利方法 —— UI 自己知道要显示 `Body`/`Needs`，用 `get_component(Body)` 取；生成日志靠各组件提供 `debug_state()` 拼接。**加一个新组件不必回来改基类。**
> `move_to()` / `_place_at()` 返回 `bool`：越界或被占时**报错并拒绝**，不会留下 `coord` 与占格不一致的状态。

**基类只放这些东西**，因为它们对 100% 的生物都成立：我是谁、我在哪、我还活着吗、我挂了哪些组件、我占哪一格、我每帧怎么推进。

**`BodyPart`**（`body_part.gd`，RefCounted）

| 字段 | 类型 | 说明 |
|---|---|---|
| `def` | `BodyPartDef` | 指向定义 |
| `hp` | `float` | 初始化为 `def.max_hp` |
| `bleeding` | `float = 0.0` | **每秒掉血量**。部位归零时由 `Body` 主动清零（出血结束） |
| `_half_logged` / `_down_logged` | `bool` | 日志去重标记 |

方法：`ratio()`（**带除零保护**：`max_hp <= 0` 返回 0 而非 NaN）/ `function_ok()` / `apply_damage(amount)` / `add_bleeding(amount)` / `status_text()`（失能 / 出血 / 正常）

> `infection` 字段**已删除**。它以前没有任何代码写入，导致 `status_text()` 的「感染」是个永远不出现的死分支 —— 留着比删掉更容易骗人。感染将由计划中的**状态效果系统**（CDDA 二维键，效果 × 部位）统一实现。

**`Need`**（`need.gd`，RefCounted）

| 字段 | 类型 | 说明 |
|---|---|---|
| `def` | `NeedDef` | 指向定义 |
| `value` | `float` | 初始化为 `def.max_value` |
| `_depleted_logged` | `bool` | 日志去重 |

方法：`ratio()`（**带除零保护**：`max_value <= 0` 返回 0 而非 NaN）/ `is_depleted()`

### 4.4 组件协议

**`CreatureComponent`**（`class_name CreatureComponent extends RefCounted`，`components/creature_component.gd`）

| 成员 | 签名 | 默认 | 说明 |
|---|---|---|---|
| `creature` | `Node` | `null` | 宿主，由 `Creature` 装配时注入 |
| `setup(host)` | `void` | 记录宿主 | 覆写时必须 `super.setup(host)` |
| `requires()` | `Array` | `[]` | 声明依赖的其他组件（**返回脚本数组**，如 `[GridMover]`） |
| `is_lethal()` | `bool` | `false` | **协议 1**：报告「宿主是否已到死境」 |
| `lethal_reason()` | `String` | `""` | **协议 1**：死因说明，写进死亡日志（基类取第一个非空项） |
| `allows_movement()` | `bool` | `true` | **协议 2**：本组件是否允许宿主移动。断腿/被捆/眩晕都重写它 |
| `debug_state()` | `String` | `""` | **协议 3**：生成日志里的一段自述。空串 = 没什么可报 |
| `tick(dt)` | `void` | `pass` | 每帧推进，`dt` 来自 TimeSystem |
| `save_data()` | `Dictionary` | `{}` | `[休眠]` 无调用方（还没有存档系统） |
| `load_data(data)` | `void` | `pass` | `[休眠]` 同上 |

> 三个协议合起来解决同一类问题：**基类不该认识任何具体组件**。
> 少一个，就会冒出「基类里写 `get_component(Body)`」这种回头债。

**装配机制**（`Creature._build_components()`）：
`def.component_scripts` 里是 `Script` 对象 → 逐个 `.new()` → `setup(self)` → **以 `s.resource_path` 为键**存进 `_components`。
`get_component(Body)` / `has_component(Body)` 都走 `script.resource_path` 查询。

> `[已知问题]` 用**脚本资源路径**当键，意味着 `component_scripts` 里必须填 GDScript 资源本体（编辑器里拖 `.gd` 文件），不能填别的东西。`CreatureDef.validate()` 会检查这一点。

**依赖校验**：已从「只 `push_warning`」升级为 `CreatureDef.validate()` 的一部分 —— `requires()` 声明的是**硬依赖**，缺了就是配置错误，会在**启动期报错**，并在生成期**拒绝生成**该生物。

### 4.5 具体组件（当前只有 4 个）

| 组件 | 类名 | 需要谁 | 提供什么 |
|---|---|---|---|
| `grid_mover.gd` | `GridMover` | 无声明（**也不认识 Body** —— 靠 `allows_movement()` 协议问） | 能不能移 / 执行移动 |
| `wander_brain.gd` | `WanderBrain` | `requires() -> [GridMover]` | 何时走、往哪走 |
| `body.gd` | `Body` | 无 | 部位 + 出血 + 总血 + 死境上报 + 移动否决 |
| `needs.gd` | `Needs` | 无 | 需求集合的衰减 + 见底致死 + 恢复 API |

**`GridMover`**
- `DIRS`（const）：上下左右四向，不再每次调用新建数组
- `is_cell_free(c) -> bool`：在界内 且（格上无 content 或 content 是自己）
- `free_directions() -> Array[Vector2i]`：四向中可走的方向。**不能移动时返回空数组**（与 `try_step()` 保持一致，免得大脑白挑一次再被拒）
- `try_step(offset) -> bool`：先 `can_move()`，再交给 `creature.move_to()`（由后者做边界/占位校验并返回结果）
- `can_move() -> bool`：**遍历所有组件问 `allows_movement()`，全票通过才放行**

> 职责边界：**GridMover 只管「能不能移」，不管「往哪移」**。决策归大脑组件。
> 更关键：`can_move()` **不再 `get_component(Body)`**。断腿的否决来自 `Body.allows_movement()`，所以将来加「被绳索捆住 / 眩晕 / 负重超限」都不必回来改这个文件。

**`WanderBrain`**
- `step_interval: float = 0.8` `[占位]`、`interval_jitter: float = 0.3` `[占位]`
- `_timer` 递减；≤0 时从 `free_directions()` 里**随机挑一个**走一格，然后重置计时器
- `_reset_timer()`：`maxf(step_interval + creature.rng.randf_range(-jitter, jitter), 0.05)`
- 随机数走**宿主的 `creature.rng`**，不用全局 RNG（可复现性）
- `[已知问题]` 它**不记上一次的方向**，所以没有惯性/对齐（v2 方案要用 `last_dir` 做鱼群对齐，见 §8）

**`Body`**
- `parts: Array[BodyPart]`，`setup` 时从 `def.body_parts` 实例化
- `tick(dt)`：**事件驱动** —— 只处理 `bleeding > 0 且 hp > 0` 的部位（`hp -= bleeding * dt`，下限 0）；部位归零时**主动把 `bleeding` 清零**（出血结束）。没有出血时一个部位都不碰
- `hurt(part, damage, bleed) -> void`：**唯一的受伤入口**。调试/将来的战斗/坠落都走它，保证「扣血 → 状态变化 → 日志」只有一套逻辑
- `_note(part)`：部位跨阈值时打日志（去重，不刷屏）
- `is_lethal() -> bool` → `is_vital_down()`：**只要有 `def.is_vital` 为真且已失能的部位，就报告死境**
- `lethal_reason() -> String`：如「头 要害失能」
- `allows_movement() -> bool` → `can_locomote()`
- `debug_state() -> String`：`血=380/380 部位{…}`
- `can_locomote() -> bool`：若部位里存在 `stance` 类肢体，则要求**至少一条还活着**；若**没有任何** `stance` 肢体（鱼/蛇），返回 true（不受限）
- `total_hp()` / `total_max()`：各部位求和
- `part_by_id(id)`、`random_wound()`（调试按钮用，走 `hurt()` 入口）

> **`Body` 绝不调用 `creature.die()`。** 它只通过 `is_lethal()` 报告事实。执行由 `Creature.check_dead_state()` 统一接线。

**`Needs`**
- `needs: Array[Need]`，`setup` 时从 `def.active_needs` 实例化
- `tick(dt)`：每个需求 `value -= drain_rate * dt`（下限 0），见底时打一次日志（致命需求会标注「（致命）」）
- `is_lethal() -> bool`：**标了 `depleted_is_lethal` 的需求见底 = 死境**
- `lethal_reason() -> String`：如「口渴 耗尽」
- `restore(id, amount) -> bool`：恢复某项需求（将来的吃/喝/取暖/睡觉都调它）；返回 false = 该需求没激活
- `need_by_id(id)` / `debug_state()`：`需求{…}`
- **见底有后果了**：饥/渴/保暖耗尽会致死，堵上了「饿不死也渴不死」的洞。
  `[已知问题]` 疲劳**没有**致死（正确 —— 它应该带来减速/强制休息），但**减速还没实现**，所以疲劳目前仍无后果。

---

## 五、系统清单（已实现）

### 5.1 网格系统 `[已实现]`

- 纯逻辑层，**不自渲染**。画面由 `MapRenderer`（§5.8b）读它画出来。
- 40×24 共 960 格在启动时全部实例化。
- 占位机制：生物所在格的 `Cell.content` 指向该 `Creature`。
- 释放时（`NOTIFICATION_PREDELETE`）把 `Cell.content` 交还成 null，避免「清屏后该格仍被认为被占」。
- `[未决]` 地形系统：`Cell.terrain` 存在但无人写入，还没有任何地形类型。

### 5.2 时间系统 `[已实现]`

- 见 §3.3。
- 订阅方式：`Creature._ready()` 里 `TimeSystem.tick.connect(_on_tick)`。
- **停机机制**：`die()` 里 `TimeSystem.tick.disconnect(_on_tick)` —— 死掉的生物不再被 tick，而不是「还在 tick 但每帧 if not alive: return」。
- 对象被 `queue_free` 时连接由引擎自动断开。

### 5.3 生死系统 `[已实现]` ★核心架构

**三段分离，照搬 CDDA `creature.h`：**

| 阶段 | 函数 | 归属 | 职责 |
|---|---|---|---|
| 判定 | `is_dead_state()` | `Creature` | 遍历所有组件问 `is_lethal()`，**任一为真即真** |
| 接线 | `check_dead_state()` | `Creature` | `if is_dead_state(): die()`。**每帧 tick 末尾只调这一次** |
| 执行 | `die()` | `Creature` | **全项目唯一把 `alive` 改成 false 的地方** |

`die()` 做的事，按顺序：
1. `if not _alive: return`（**幂等**：多个要害同时归零也只死一次）
2. 取死因 `lethal_reason_text()`
3. `_alive = false`
4. `TimeSystem.tick.disconnect(_on_tick)`（停机）
5. `_release_cell()`（**交还所占格子** —— 否则尸体变成看不见的堵路石）
6. `_sprite.modulate = DEAD_COLOR`（变暗）
7. `Log.ev("死亡", "…（死因）…")`
8. `EventBus.creature_died.emit(self)`

> 死因来自组件的 `lethal_reason()`，所以日志能说清是「头 要害失能」还是「口渴 耗尽」，不再一律写成「要害失能」。

`_on_tick(dt)` 的全部内容：
```gdscript
for comp in _components.values():
    comp.tick(dt)
check_dead_state()
```
**循环里没有任何 `alive` 判断。** 判定与接线都在基类，组件不必记得自己查。

**当前存在的死法有两种**：
1. `Body` 报告「要害部位（头 或 躯干）被打到 0」。
2. `Needs` 报告「标了 `depleted_is_lethal` 的需求耗尽」（饥饿 / 口渴 / 保暖）。

> 扩展方式：**加一种死法 = 加一个组件并重写 `is_lethal()` + `lethal_reason()`**。`die()` 与 tick 循环一行都不用动。

### 5.4 身体与伤害系统 `[已实现]`

- 部位清单来自数据（猪有 6 个：头 / 躯干 / 前左腿 / 前右腿 / 后左腿 / 后右腿）。
- **总血 = 各部位 hp 之和**（不是独立血条）。所以「总血」是派生量，不是存储量。
- 伤害走**唯一入口** `Body.hurt(part, damage, bleed)`；调试、将来的战斗/坠落都调它。
- 出血：`BodyPart.bleeding`（每秒）在 tick 里扣该部位 hp。**部位归零时出血自动清零**（出血结束）。`[未决]` 还没有主动止血/包扎。
- `[未决]` 感染 —— `infection` 字段已删除，将由状态效果系统实现（见 §8.3）。
- `[未决]` 部位连接关系（父/对侧/子部位）、加权肢体类型 —— 见 §8.3。

**部位状态 → 能力的映射（涌现式，不查表）**：

| 部位状态 | 后果 | 实现处 |
|---|---|---|
| `is_vital` 部位失能 | 宿主死亡 | `Body.is_lethal()` |
| 所有 `stance` 肢体失能 | 不能移动 | `Body.allows_movement()` → `GridMover.can_move()` |
| `grasp` 肢体失能 | `[已拍板未实现]` 伤臂不能制作 | `grasp` 机制已备，但**当前没有任何物种使用它**（猪是四足，不抓握）；制作系统尚未存在 |

### 5.5 需求系统 `[已实现]`

- 需求清单来自数据（猪已挂 4 个：饥饿 / 口渴 / 保暖 / 疲劳）。
- `[占位]` 全部初始 100，每秒衰减：饥饿 0.6 / 口渴 0.9 / 保暖 0.3 / 疲劳 0.4。
  → 按当前值，口渴约 111 秒见底，饥饿约 167 秒，疲劳 250 秒，保暖约 333 秒（游戏内秒）。**猪不进食的话约 111 秒就会渴死。**
- **见底有后果**：标了 `depleted_is_lethal` 的需求（饥饿/口渴/保暖）耗尽 → `Needs.is_lethal()` 上报 → 宿主死亡，日志写明死因。
- 恢复走 `Needs.restore(id, amount)` —— 将来的吃/喝/取暖/睡觉都调它。**目前还没有任何东西会调它**（没有食物、没有交互）。
- `[已知问题]` 疲劳**不是**致命需求且**没有减速效果** → 疲劳目前实质无后果。
- `[未决]`「保暖」是**恒定速率倒计时**，与环境/火/天气无关。名字已从「体温」改为「保暖」以反映真实语义（一个会持续流失的储量），真正的环境驱动温度系统尚未设计。
- `[已拍板未实现]` 水陆差异（鱼不挂 `Needs`，因此不需要口渴）——机制是通的（按物种挂组件），只是没有第二个物种来证明。

### 5.6 移动与决策 `[已实现]`

- 移动表现：**瞬移 + 停顿**（选定风格）。走一格即 snap 到位，间隔 0.8s ± 0.3。
- 决策：`WanderBrain` 每间隔随机挑一个可走方向。
- 移动前置：腿全断 → `can_move()` 为 false → 走不动（会长间隔空转）。
- 不会重叠：目标格必须空闲才进。
- 不会越界：`is_cell_free()` 查 `in_bounds`。
- `[休眠]` `Creature.move_to()` **没有 `on_move()` 钩子**。CDDA 的 `move_to()` 会调 `on_move()`，让「留脚印/噪音/领地判定」挂上去而不改移动代码。我们将来加行为会想改 `move_to()` —— 应该先留钩子（见 §8.3）。

### 5.7 日志与事件 `[已实现]`

**`Log`**（Autoload）
- `enabled: bool = true`
- `categories: Dictionary`，默认开关：生成 / 受伤 / 失血 / 死亡 / 需求 / 情绪 = `true`；**移动 = `false`**（刷屏）
- `ev(category, msg)` → `print("[t=%5.1f][%s] %s")`，时间戳取 `TimeSystem.elapsed`
- `set_category()` / `toggle()` `[休眠]`：无调用方（没有日志过滤界面）

**`EventBus`**（Autoload）

| 信号 | 参数 | 发射处 | 接收处 |
|---|---|---|---|
| `creature_clicked` | `creature: Node` | `Creature._unhandled_input()` | `CreatureInspector` |
| `creature_spawned` | `creature: Node` | `main.gd::_spawn_pig()`（生成成功即广播） | `Minimap` |
| `creature_died` | `creature: Node` | `Creature.die()` | `CreatureInspector`（切换尸体视图） |

> 信号「不持有引用」，对象释放后连接自动断（Godot 机制）——这是选 EventBus 而非直接引用的原因。

### 5.8 UI 系统 `[已实现]`（调试壳）

**`DebugToolbar`**（Control，CanvasLayer 左上角）
- 按钮「刷猪」→ 委托 `get_tree().current_scene.spawn_pig_random()`（**工具栏自身不持有生成逻辑**）
- 按钮「清屏（猪）」→ 遍历主场景子节点，`queue_free()` 所有 `Creature`
- `[已拍板未实现]` 停止/暂停、倍速按钮（`TimeSystem.paused/speed` 已备好，只缺 UI）

**`CreatureInspector`**（PanelContainer，悬浮窗）
- **紧凑网格布局**（实测面板 320×251，改前竖排 10 行为 277×602）：
  - 标题栏（**可拖动窗口**）→ 名称+坐标同一行 → 死亡提示 → 总血行（PixelBar 12 段）→ 「部位」小标题与网格同行：**3 列 × 2 行**格子 → 「需求」小标题与网格同行：**4 列 × 1 行**格子 → 「调试：随机致伤」按钮
  - 每个格子 = 上排名字 + 下排（像素条 6 段 + 可选状态文字）；列数由 `PARTS_COLS`/`NEEDS_COLS` 常量定，加部位/需求超出行数会自动往下长（GridContainer 行为）
- **字号纪律**：全局主题 22，但格子内容用 `_small_label()` 设 **11px**（`CELL_FONT_SIZE`）
  - **实测坑（Godot 4.7）**：父控件上的 `add_theme_font_size_override` **不会向下继承**，子控件仍读到主题值 22 —— 必须逐控件设
  - 像素字体 11px 原生 + import 里 `antialiasing=0`，只有 11/22/33 整数倍不糊；别用 14/16 这类中间值
- 订阅 `EventBus.creature_clicked` 决定显示谁；订阅 `TimeSystem.tick` 实时刷新
- 数据全部来自组件（`get_component(Body)` / `get_component(Needs)`）→ **换任何物种都能显示**，UI 不认识「猪」
- 初始位置 `(32, 104)`，默认 `hide()`
- **活体 / 尸体两种形态**由唯一事实 `is_alive()` 决定，由 `_apply_form()` 一次性应用：
  - 活体：显示总血块 + 需求块，隐藏死亡标签
  - 尸体：隐藏总血块 + 需求块（**真的隐藏**），显示「── 已死亡（死因）· 模拟已停止 ──」
  - 部位行**两种形态都显示** —— 死后数值本来就冻结了，正好当验尸
- 订阅 `EventBus.creature_died`，选中的生物一死就切尸体视图（另有 `_refresh()` 兜底校正）

**`PixelBar`**（`class_name PixelBar extends Control`，自绘）
- `segments`（段数）、`cell: Vector2(8,8)`（每段方块）、`gap: int = 2`
- `FILL = Color(0.82,0.82,0.82)` 灰白（满）、`EMPTY = Color(0.22,0.22,0.22)` 暗灰（空）
- `set_ratio(r)` → 钳制 0..1 + `queue_redraw()`
- `mouse_filter = IGNORE`（不抢拖窗事件）
- 用**方块**而非进度条，贴合像素美术基调

**`game_theme.tres`**（全局主题，`gui/theme/custom`）
- `default_font = HYPixel11pxU-2.ttf`（汉字像素字体，11px 规格）
- `default_font_size = 22`
- `Label/Button` 字色 `Color(0.82,0.82,0.82)`；Button 悬停 `0.96`、按下 `0.62`

**`Minimap`**（`class_name Minimap extends Control`，`ui/minimap.gd`）`[已实现]` — 右上角小地图（DF 式）
- 整个逻辑网格的缩略图：每格 **4px**（40×24 → 160×96 + 4px 边距），外框**白色** `Color.WHITE`（用户拍板，不要暗金）
- 颜色约定与主画面同一套：**黑=陆地**；格子被生物占 → 画 `def.map_color`
- 位置：`_ready` 里 `set_anchors_and_offsets_preset(PRESET_TOP_RIGHT, MINSIZE, 8)` → 跟窗口右上角走（含窗口拉伸后）
- **重画时机 = 信号**：`TimeSystem.tick`（模拟推进）/ `creature_spawned` / `creature_died`。不每帧扫，暂停时小地图跟着冻结
- **只读 GridManager**，不持有任何生物引用；首次布局可能早于 Autoload 就绪（实测），`_get_minimum_size`/`_draw` 都有 grid 空守卫

### 5.8b 地图渲染 `[已实现]`（色块地图，2026-09-19 起）

**`MapRenderer`**（`class_name MapRenderer extends Node2D`，`render/map_renderer.gd`）
- 把逻辑网格画出来：每格 32px 色块。**不画网格线**（用户拍板：主画面不要格线）
- 颜色表 `TERRAIN_COLORS`：terrain 名 → Color，**查不到一律黑**（含初始值 `&"unknown"`）→ 将来加地形类型 = 加一行数据，不改代码
- 当前 terrain 恒为 unknown → 这层画出来全黑、与清屏色一致（**暂时"看不见"**，等地形系统写入 terrain 后立刻显形）
- **只读不写**：树序在生物之前（生物画在上层）；静态 `_draw()` 一次（地形不变就不重画）
- 颜色约定（用户拍板）：**黑 = 陆地**；生物的表现归 `Creature` 自己（见下）

**生物表现（`Creature._render()` + `_draw()`）**：**有图就用图**（猪 = `pic/pig.png` 剪影，Sprite2D 缩放成一格，`pig.tscn` 里有 `Sprite2D` 节点）；没配 `texture` 的物种才用 `map_color` 色块兜底（格子中央 `BODY_SIZE=20px`），死亡变 `DEAD_COLOR`。小地图不看图，一律用 `def.map_color`。

**点击选取的实现**（因为 `input_pickable` 赋不了值）：
```gdscript
func _unhandled_input(event: InputEvent) -> void:
    var world_pos: Vector2 = vp.get_canvas_transform().affine_inverse() * event.position
    if GridManager.grid.world_to_grid(world_pos) == coord:
        EventBus.creature_clicked.emit(self)
        vp.set_input_as_handled()
```
即：屏幕坐标 → 世界坐标 → 反算格子 → 与自身 `coord` 比对。复用网格系统，不依赖 picking 属性。

> **用 `_unhandled_input` 而不是 `_input`** 是刻意的：Godot 的输入顺序是 `_input` → GUI(Control) → `_unhandled_input`。被面板/按钮消费掉的点击**不会到达** `_unhandled_input`，所以点检视窗不会「穿透」选中窗下的生物。`set_input_as_handled()` 保证一次点击只选一个。

### 5.9 数据文件现状 `[已实现]`

**部位（6 个 `.tres`）**

| 文件 | id | label | max_hp | is_vital | limb_types |
|---|---|---|---|---|---|
| `part_head.tres` | head | 头 | 40 | **true** | — |
| `part_torso.tres` | torso | 躯干 | 120 | **true** | — |
| `part_foreleg_l.tres` | foreleg_l | 前左腿 | 50 | false | `["stance"]` |
| `part_foreleg_r.tres` | foreleg_r | 前右腿 | 50 | false | `["stance"]` |
| `part_hindleg_l.tres` | hindleg_l | 后左腿 | 60 | false | `["stance"]` |
| `part_hindleg_r.tres` | hindleg_r | 后右腿 | 60 | false | `["stance"]` |

→ 猪总血 **380**，其中要害部分 160、四条腿 220。

> **猪是四足动物，所以用四足身体模板。** 早期它套的是「头/躯干/左臂/右臂/左腿/右腿」的人类模板，还给「手臂」配了 `grasp`（抓握）—— 猪并不会抓握，数据在说谎。现已修正为四条腿。
> `grasp` 机制本身保留（将来给人形/猛禽用），但目前**没有任何物种使用它**。
> 这只猪的例子正是 `BodyPlanDef`（DF 式具名身体模板，见 §8.3-3）要解决的问题：眼下每加一个四足物种都得再复制 6 份 `.tres`。

**需求（5 个 `.tres`）**

| 文件 | id | label | max_value | drain_rate | 是否挂在猪 |
|---|---|---|---|---|---|
| `need_food.tres` | food | 饥饿 | 100 | 0.6 | 是 |
| `need_water.tres` | water | 口渴 | 100 | 0.9 | 是 |
| `need_warmth.tres` | warmth | 保暖 | 100 | 0.3 | 是 |
| `need_fatigue.tres` | fatigue | 疲劳 | 100 | 0.4 | 是 |
| `need_breath.tres` | breath | 憋气 | 100 | 3.0 | **否**（文件存在，`pig_def` 未引用） |

**`pig_def.tres`**
- `display_name = "猪"`，`texture = pic/pig.png`（**渲染已不使用**，保留在数据里），`map_color` 未配置 = 默认白
- `component_scripts = [grid_mover, wander_brain, body, needs]`（**4 个**）
- `body_parts` = 上述 6 个
- `active_needs` = food / water / warmth / fatigue（**4 个**，不含 breath）

**`pig.tscn`**：根 `Node2D` + `creature.gd`，`def = pig_def.tres`，子节点 `Sprite2D`（空纹理，`_render()` 从 `def.texture` 赋上并缩放成一格）。

**图标缩放**：`pig.png` 是 512×512；`_render()` 里 `scale = CELL_SIZE / texture.get_width()` = 32/512 = **0.0625** → 正好一格。

**`main.gd`**：`_ready()` 打 4 行网格自检 → `_spawn_pig(Vector2i(20,12))`；`_spawn_pig()` 成功后 `EventBus.creature_spawned.emit(pig)`；`spawn_pig_random()` 收集所有空格后随机挑一个生成。
> 注意：`pig.coord` **必须在 `add_child` 之前设好**，否则 `_ready()` 时落格会用 `Vector2i.ZERO`。

---

## 六、关键规则与约定

> 这一节是**写代码时必须遵守的**纪律。违反它 = 架构腐化。

### 6.1 三条归属线 + 判定三问

对任何「要不要加进基类」的疑问，问三句：

1. **是不是每个生物都无条件需要它？** 是 → 基类；否 → 往下问。
2. 它一种是**能力/需求**（有状态、会持续做事），还是**一堆数值**？ 能力/需求 → **组件**；数值 → **数据**。
3. 它是不是只在某一类物种成立、且行为差异极大？ 优先 **组合**，别急着开子类。

| 归属 | 机制 | 装什么 | 例子 |
|---|---|---|---|
| **基类字段** | `Creature` 成员 | 100% 通用状态 | `coord` / `alive` / 组件表 |
| **组合组件** | `CreatureComponent` | 「有时候需要」的能力 | 部位、需求、移动、大脑、家的位置 |
| **数据** | `*Def` Resource | 数值、配置、清单 | `max_hp` / `is_vital` / `drain_rate` / 挂哪些组件 |

**反例（永远不许再犯）**：「家的位置」写进基类 → 每条鱼都背一个没用的 `home_position`。基类会随「部分物种特性」不断胖成 god class。

### 6.2 组合优于继承

- 继承的翻车案例：企鹅是鸟不会飞、鲸会游不是鱼、飞鱼像鸟又像鱼 → 继承树会裂。
- **物种差异靠「挂哪些组件 + 配什么数据」，不靠继承树。**
- 真要继承，只留一层稳定原型，且子类里也不能塞「部分物种才要」的东西。当前代码**零子类**，只有 `Creature` 一个基类 —— 这是正确状态。

### 6.3 生死纪律（最重要的一条）

- `die()` 是全项目**唯一**把 `alive` 改成 false 的地方。
- **组件永远不许调用 `creature.die()`。** 组件只重写 `is_lethal()` 报告事实。
- 判定（`is_dead_state`）与接线（`check_dead_state`）都在基类；tick 循环里**不放任何 `alive` 判断**。
- 死掉的生物靠 `TimeSystem.tick.disconnect()` 摘出时钟，不是靠每帧自检。

### 6.4 数据纪律

- 内容（数值/清单/挂哪些组件）写在 `.tres`，**不硬编码在脚本里**。
- `.tres` 之间用**独立文件 + `ExtResource` 按路径引用**；**不许用内联子资源**（见 §2.4 坑 1）。
- `Cell.terrain` 这类**预留字段**：可以先留，但**必须承认它是休眠的**，不许在文档里宣称已完成。
  更进一步的纪律：**如果一个字段会让某段代码说谎，就删掉它**（`BodyPart.infection` 就是这样被删的 —— 它让 `status_text()` 里出现了一个永远不可能的「感染」状态）。

### 6.5 解耦纪律

- 系统/UI 之间不直接互相调用 → 走 `EventBus` 信号。
- 逻辑不认识 UI：`Creature` 不知道有检视面板；面板自己订阅 `creature_clicked`。
- UI 不认识物种：面板读 `get_component(Body)` / `get_component(Needs)`，换物种不改 UI。
- 工具栏不认识生成逻辑：委托 `Main.spawn_pig_random()`。
- 移动不认识决策：`GridMover` 只管执行，`WanderBrain` 只管选方向。
- 组件之间靠 `requires()` **显式声明**依赖，而不是到处 `get_node`。

### 6.6 变更纪律（只新增、少改动）

优先**在现有结构上增加功能**，而不是改动已有代码。判据：
- 加一个物种 = 新增 `CreatureDef` + 场景，**不碰任何系统代码**。
- 加一种死法 = 新增组件重写 `is_lethal()`，**不碰 `die()` 与 tick 循环**。
- 加一种行为 = 换一个 Brain 组件，**不碰基类、不碰移动组件**。
- 加一种需求 = 新增 `NeedDef` + 挂进物种数据，**不碰 `Needs` 组件代码**。

### 6.7 数值纪律

- 所有数值初值都是 `[占位]`，**必须跑出来再调**，不许凭想象定平衡。
- 任何「待定」的数值就在文档里标 `[占位]`，不要假装它是最终值。
- 出处不明的机制**不许写**。要写就注明来源（源码 / 访谈 / 实测），来源不明确的标注为「未核实」。

### 6.8 表现与逻辑分离

- 逻辑层不依赖渲染。删掉 `MapRenderer`、`Minimap` 和 `Creature._draw()`，模拟照样跑。
- 表现入口有三处，全部**只读**：`MapRenderer._draw()`（格子）、`Creature._draw()`（生物色块）、`Minimap._draw()`（缩略图）。`def.texture` 可以是 null。

### 6.9 加一只新物种的标准流程

1. 备部位：新建 N 个部位 `.tres`（或将来的 `BodyPlanDef` 具名模板，见 §8.3）。
2. 备需求：新建/复用需求 `.tres`。
3. 新建 `X_def.tres`：`script = creature_def.gd`；填 `display_name`、`texture`（可空，空则主画面显示 `map_color` 色块）、`map_color`（小地图色点，默认白）、`component_scripts`（拖 `.gd` 脚本本体）、`body_parts`、`active_needs`。
4. 新建 `X.tscn`：根 `Node2D` + `creature.gd`，`def = X_def.tres`，加子节点 `Sprite2D`（配了 texture 才需要）。
5. 在 `main.gd` 或调试工具栏里生成它。
6. **不改任何系统代码。** 若做不到第 6 条，说明架构有问题，先改架构再继续。

### 6.10 校验纪律（这一条是本项目最容易自欺的地方）

1. **校验器必须被证明过能抓到错。** 写完一个校验器，要**故意注入一个错误**跑一遍，看到它真的喊出来，才算完成。
   否则你得到的可能是个「永远说通过」的校验器 —— 比没有校验更危险，因为它会给你虚假的安全感。
2. **「0 条问题」和「根本没查到东西」必须长得不一样。** 扫描类代码遇到「一个目标都没扫到」时应当**报错**，而不是安静地返回 0。
   `DefValidator` 就专门守这一条：扫不到 `.tres` 会直接 `push_error`。
3. **统计口径要写对。** 「N 个文件」指的是扫到的总数还是有问题数？写错就会得出「扫描 0 个文件、0 条问题」这种自我安慰的输出。
4. **改完必须实跑，并给出断言式输出。** 不接受「我认为应该没问题」：临时插一段打印（`[验证] xxx=…（应 …）`），跑出来看到实际值，再删掉临时代码。
5. **临时验证代码用完即删。** 留在里面就是下一次事故的种子。

---

## 七、数值现状总表（全为占位）

| 项 | 值 | 位置 |
|---|---|---|
| 格边长 | 32 px | `Grid.CELL_SIZE` |
| 网格尺寸 | 40 × 24（= 960 格 / 1280×768 px） | `GridManager._ready()` |
| 猪总血 | 380（头 40 + 躯干 120 + 前腿 50×2 + 后腿 60×2） | 6 个 `part_*.tres` |
| 要害部位 | 头、躯干 | `is_vital = true` |
| 支撑肢体 | 四条腿（`stance`） | `limb_types` |
| 抓握肢体 | **当前无物种使用**（机制保留） | `limb_types` |
| 需求初值 | 均 100 | `need_*.tres` |
| 饥饿 / 口渴 / 保暖 / 疲劳 衰减 | 0.6 / 0.9 / 0.3 / 0.4 每秒 | `drain_rate` |
| 憋气衰减 | 3.0 每秒（**未启用**） | `need_breath.tres` |
| 游荡间隔 | 0.8 s ± 0.3 s（下限 0.05 s） | `WanderBrain` |
| 调试致伤 | 15–45 伤害 + 1–4 出血/秒 | `Body.random_wound()` |
| 死亡色 | `Color(0.32,0.32,0.32)` | `Creature.DEAD_COLOR` |
| 像素条 | 块 8×8、间距 2、满 `0.82` 灰白 / 空 `0.22` 暗灰 | `PixelBar` |
| 主题字号 | 22（字体 HYPixel11pxU-2） | `game_theme.tres` |

---

## 八、尚未实现 / 已验证但未接线

### 8.1 已拍板但未实现

| 项 | 决定内容 | 落点 |
|---|---|---|
| 暂停 / 倍速控制 | 机制已有（`TimeSystem.paused/speed`），**缺 UI** | 加按钮即可 |
| 伤臂不能制作 | `grasp` 机制已备，缺制作系统与被使用的数据 | 待 Crafting 组件 |
| 需求见底的后果 | **已实现**（见底即死，`depleted_is_lethal`） | 完成后可改为渐进掉血 |
| 疲劳的后果 | 应为减速 / 强制休息，**目前完全无后果** | 待减速机制 |
| 主动止血 / 包扎 | 出血只会自己流干，没有处置手段 | 待医疗 + 物品 |
| 性格与行为系统 | 6 维性格（攻/勇/强/活/焦/同）→ 加权驱动融合（v2 方案） | 见 §8.2 的未决项 |
| UI 显示性格/状态 | 检视面板加性格与状态行 | 随性格系统 |

### 8.2 未决（等你拍板，不许当既定）

来自《性格与身体表现_实现方案.md》：

1. **身体横截面/侧视图** —— 已由你说「先算了」，但记录为**暂缓**而非取消。
2. 性格生成用**雨世界式关联公式**（`bravery/energy/sympathy` 随机 + `aggression/dominance/nervous` 推导）还是**纯随机**？
3. 社交先只做**同种猪之间**？（目前没有其他物种）
4. 攻击性先只做「**驱赶 / 挤开**」不做伤害？（还没有战斗系统）
5. 合群度权重、驱动权重、感知半径 R —— 先给**占位默认值**跑效果再调？

来自《生物大类基础属性.md》：
6. **意识 / 击晕**：做成组件（默认挂）还是不做？（「血没空也可能被击晕」是独立维度）
7. `Stats`（力量/敏捷/体质）：独立组件，还是并进数据不做组件？

### 8.3 研究文档提出、尚未采纳的候选项

来源：《参考游戏源码研究.md》§5.2 / §5.3。**全部只是「建议」，未拍板。**

**修正类（改既有逻辑的读取方式）**
1. ~~定义校验 pass（引用错误要**报错**而非 `push_warning`）~~ **已完成** —— `CreatureDef.validate()` + `DefValidator` 启动期扫描，见 §4.2
2. `Registry`（字符串 id → Script）+ **组件带参数**（当前是裸 `Array[Script]`，且参数不可覆写 → 想调 `wander.step_interval` 只能改脚本默认值）
3. `BodyPlanDef` 具名身体模板（DF `[BODY:模板A:模板B]`）→ 狼/鹿共享 `QUADRUPED`，不再复制 6 个 `.tres`
4. 部位连接关系（`connected_to` / `opposite_part` / 加权 `limbtypes`）

**新增类**
5. `on_move()` 钩子（`move_to()` 末尾发 `EventBus.creature_moved`）→ 留脚印/噪音/领地
6. `SimulationLevel` + `SimulationBudget`（雨世界抽象↔具现两级模拟 + 逐物种性能开销）→ 规模上来必须做
7. 状态效果系统（CDDA 二维键 效果×部位，带 duration/intensity/permanent）→ 替代 `BodyPart.bleeding: float` 这种简化
8. 关系与社交记忆（`like/fear/temp_like/temp_fear/know`，临时值会衰减）
9. 出血写进世界（格子级血渍），而不是只减一个数
10. 定义继承 `copy_from` + `abstract`

**明确不做**：DF 式「文本 token 手术」继承（无结构校验，易成难维护的硬壳）。

**建议的落地顺序**（依赖少、风险低优先）：①定义校验 → ②Registry + 组件参数 → ③BodyPlanDef → ④on_move 钩子 → ⑤SimulationLevel → ⑥状态效果 → ⑦关系/社交 → ⑧性格驱动行为。
> 第 ①（数据化要害/肢体 + 判定/接线/执行分离）**已完成**，见 §5.3。
> 第 ②③项之间的「定义校验」**也已完成**（`validate()` + `DefValidator`，见 §4.2）。
> 另加一项未在原文中的已做修正：**基类去具体组件化**（`debug_state()` / `allows_movement()` / `lethal_reason()` 三个协议），见 §4.4。

### 8.4 休眠代码清单（代码在，但没接线）

| 位置 | 内容 | 状态 |
|---|---|---|
| `Grid` | `has_cell()` / `for_each_cell()` | 无调用方 |
| `Body` | `part_by_id()` | 无调用方（`random_wound()` 直接用下标取） |
| `Needs` | `restore(id, amount)` | 无调用方 —— **目前没有任何食物/交互系统会调它** |
| `CreatureComponent` | `save_data()` / `load_data()` | 无调用方（无存档系统） |
| `TimeSystem` | `paused` / `speed` | 逻辑读它，**无任何地方设置**（没有暂停/倍速 UI） |
| `Log` | `set_category()` / `toggle()` | 无调用方（没有日志过滤界面） |
| `Cell` | `terrain` | 被 `MapRenderer` 读（画黑底），**无写入处** —— 没有地形系统，值恒为 `&"unknown"` |
| `Creature` | `rng` 的**可复现性** | 种子已按「世界种子 + 序号」派生，但**没有存档系统去读回它** |
| `pic/wolf-head.png` | 狼图标 | 未被引用 |
| `need_breath.tres` | 憋气定义 | 未被任何 `CreatureDef` 引用 |

> 已从本清单移出（现已接线）：`EventBus.creature_died`（检视面板接收）、`BodyPart.infection`（已删除）、`BodyPart.function_ok()`（`is_vital_down()` 在用）、`Need.is_depleted()`（`Needs` 在用）、`Needs.need_by_id()`（`restore()` 在用）、`CreatureInspector._dead_lbl`（尸体视图已点亮）。

> 休眠 ≠ 无用。它们多是**有意的预留**（如 `save_data` 等存档系统、`terrain` 等地形系统）。但**不许在别处宣称它们已生效。**

---

## 九、文档状态表

| 文档 | 状态 | 说明 |
|---|---|---|
| **`事实文档.md`（本文）** | **唯一事实基准** | 一切以本文为准 |
| `参考游戏源码研究.md` | **有效参考** | CDDA/雨世界/矮人要塞的源码与架构研究，带可信度标注。是「为什么这样设计」的依据，不是「现在是什么」的描述 |
| `动物类设计重构方案.md` | **部分有效** | 三归属线原则仍有效（已并入 §6.1）；但其中的组件命名（`HungerNeed`/`PartedBody`/`Stats`）与数据字段（`components: Array[StringName]`）**与实际代码不符**，以本文 §4 为准 |
| `性格与身体表现_实现方案.md` | **待拍板** | v2 方案，5 个决策未答（见 §8.2）。未写码 |
| `生物大类基础属性.md` | **历史** | 分层思路（A代谢/B身体/C状态/D物理/E认知）有参考价值；「固定字段」类表述已被组件化取代 |
| `生物模拟系统设计.md` | **已作废** | 第 2 节固定子节点树已被组件化取代（其自身头部也已注明）。仅时钟与信号部分仍有效（已并入本文） |
| `荒岛求生开发计划.md`（工程外，`D:\Desktop\GAME\`） | **部分过期** | 长期愿景与里程碑仍有参考价值；但引擎版本（写 4.6，实为 4.7.2）、目录结构、Autoload 清单、渲染器选择**均已与实现不符**，以本文 §2 为准 |

---

## 十、当前基线（一句话总结）

> **可用的**：一个数据驱动的生物模拟内核 —— 逻辑网格 + 统一时钟 + 组件化生物 + 部位化身体 + 判定/接线/执行分离的生死系统 + **启动期定义校验** + 调试 UI。
> **验证状态**：`--import` 零脚本错误；`--quit-after 400` 实跑：`[定义校验] 通过：扫描 12 个定义文件`、猪#1 生成日志（4 组件 / 380 血 / 4 需求），无运行期报错。
> **不可用的**：没有玩家、没有地图、没有物品、没有目标、没有存档 —— 还不能「玩」。
> **下一步的候选**（未拍板）：`Registry` + 组件参数 / `BodyPlanDef` / `on_move` 钩子 / `SimulationLevel` / 状态效果系统。
