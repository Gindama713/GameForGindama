# 参考游戏源码研究：CDDA / 雨世界 / 矮人要塞

> 目的：为「荒岛求生」的底层系统设计找**可验证的**先例，减少拍脑袋。
> 用途：指导后续开发，原则是**只新增、少改动、数据驱动**。

---

## 0. 方法与可信度声明（先划清哪些是源码、哪些不是）

| 游戏 | 源码是否公开 | 本文信息来源 | 可信度 |
|---|---|---|---|
| 大灾变 CDDA | **公开**（CleverRaven/Cataclysm-DDA，C++） | 直接读仓库 `src/*.h/*.cpp`、`doc/` | **A 级：一手源码** |
| 雨世界 Rain World | **不公开**（Unity，闭源） | ①Unity 官方技术访谈（Akupara 开发主管原话）②miraheze 技术页/Remix 页（大量标注内部函数名）③反编译 Modding 文档 | B 级：官方访谈 + 可交叉验证的社区技术档 |
| 矮人要塞 Dwarf Fortress | **不公开**（C/C++，Bay 12） | ①DF wiki 的 raw token 官方文档 ②DFHack 结构镜像（`df.unit.xml` 等，字段名对照 bay12 源码）③Tarn Adams 2021 StackOverflow 访谈 | B 级：raw 文档准；内部结构靠 DFHack 镜像 |

**明确没查到、不许编的部分**（本文一律留空不补）：
- CDDA `Creature::die()` 的**函数体**（文件过大被截断，只确认了签名与调用点）。
- 矮人要塞 `df::unit` / `unit_health_info` / `df::body_part` 的**完整字段表**（`df.unit.xml` 抓取被截断，只拿到 flag 位）。本文只使用已拿到的部分。

---

## 1. 大灾变（CDDA）—— 一手源码

### 1.1 类型体系：虚判定 + 安全向下转型，不靠 RTTI

`creature.h`：

```cpp
class Creature : public viewer          // 玩家/NPC/怪物共同父类
virtual bool is_avatar() const { return false; }
virtual bool is_npc() const { return false; }
virtual bool is_monster() const { return false; }
virtual Character *as_character() { return nullptr; }
virtual monster   *as_monster()   { return nullptr; }
```

子类**重写判定函数返回 true**、**重写转型函数返回 `this`**。
教训：想区分「是不是玩家」，用可重写的谓词；不要写 `if type == PLAYER` 这种散在各处的枚举比较。

### 1.2 死亡：判定 / 接线 / 执行，三段分离 ★最重要的发现

```cpp
virtual bool is_dead_state() const = 0;                 // 子类回答：我算不算死了
virtual void die( map *here, Creature *killer ) = 0;    // 子类执行：真的死
void check_dead_state( map *here );                     // 基类接线：if(is_dead_state()) die();

protected:
Creature *killer;   // 注释原文：this should be NULL unless we are dead
```

**没有 `bool dead` 成员。** 死亡相关调用点（`creature.cpp` 实际出现处）：
`deal_projectile_attack()` 里 `check_dead_state( here );`、`hallucination_die()` 里 `die( here, killer );`、`process_effects()` 里 `die(...)`。

设计意图：**加一种新的死法（饿死、中毒、出血），不需要改 `die()`，也不需要改调用 `die()` 的地方** —— 只要让新的那个系统参与 `is_dead_state()` 的判定即可。

> **这一条修正了我们上一版的做法。**
> 我们把「判定」写进了 `Body._check_vitals()`（`if is_vital_down(): creature.die()`）——组件自己判自己杀。
> CDDA 把判定上提到宿主，接线在基类，组件只负责**暴露事实**（如 `is_vital_down()`）。

### 1.3 身体部位：模板 / 实例分离，且**要害与否是数据**

`bodypart.h` 两个类型：

**模板** `struct body_part_type`：
```cpp
bodypart_str_id id;
bodypart_str_id main_part;       // 伤害判定上的父（躯干指向自身）
bodypart_str_id connected_to;    // 解剖连接上的父
bodypart_str_id opposite_part;   // 对侧部位
std::vector<sub_bodypart_str_id> sub_parts;
std::map<bp_type, float> limbtypes;   // 肢体类型 -> 权重（可同时是多种）
int  base_hp = 60;
int  heal_bonus = 0;   float mend_rate = 1.0f;
bool is_limb  = false;
bool is_vital = false;   // 要害：归零即死
```

**实例** `class bodypart`：`hp_cur / hp_max / wetness / temp_cur / wounds`。

要点：
1. **`is_vital`、`is_limb` 是数据字段**，不是代码里 `category == "head"` 这种比较。
   → 我们的 `Body.is_vital_down()` 在比字符串 `=="head"/"torso"`，`can_locomote()` 在比 `=="leg"`。这是把规则硬编码进了逻辑。
2. 部位不是扁平表，有**连接关系**（主部位/连接/对侧/子部位）与**加权多类型**。
3. 部位定义从 JSON 加载（`body_part_type::load_bp` / `reset()` / `finalize_all()` / `check_consistency()`），运行期用 `get_all()` 决定「存在哪些部位」。

### 1.4 状态效果：二维键（效果 × 部位），且类型是数据

```cpp
class effects_map : public std::map<efftype_id, std::map<bodypart_id, effect>>;
```

实例 `class effect`：
```cpp
const effect_type *eff_type;
time_duration duration; bodypart_str_id bp;
bool permanent; int intensity;
time_point start_time; effect_source source;
```

类型 `class effect_type`（JSON 加载）：`max_intensity`、`max_duration`、`flags`、`immune_flags`、`resist_effects`、`removes_effects`、`blocks_effects`、`decay_msgs`、`death_msg`、`kill_chance`、`limb_score_data` …

接口（`creature.cpp` 实测签名）：
```cpp
void Creature::add_effect( const effect_source &source, const efftype_id &eff_id,
                           const time_duration &dur, bodypart_id bp,
                           bool permanent, int intensity, bool force, bool deferred );
bool Creature::remove_effect( const efftype_id &eff_id, const bodypart_id &bp );
```

要点：**同一个效果可以落在不同部位上、各自独立计时与强度**。「出血」在 CDDA 眼里就是一种可带部位的效果实例，而不是部位对象上的一个 `float`。
→ 我们现在 `BodyPart.bleeding: float` 是简化版；状态效果系统应当另建，不要往部位上继续挂字段。

### 1.5 出血是把东西「加进世界」，不是减一个数

```cpp
void Creature::bleed( map &here ) const
{
    here.add_splatter( bloodType(), pos_bub( here ) );
}
```

教训：出血的**可见后果**是往地图上留血；数值掉血只是配套。将来做「血迹/吸引掠食者」时，入口就在这里，而不该去改掉血逻辑。

### 1.6 扩展点范本：`activity_actor` 注册表（OCC 原则的教科书实现）

`activity_actor.h`：抽象基类给全生命周期钩子
```cpp
virtual const activity_id &get_type() const = 0;
virtual void start  ( player_activity &act, Character &who ) = 0;
virtual void do_turn( player_activity &act, Character &who ) = 0;
virtual void finish ( player_activity &act, Character &who ) = 0;
virtual void canceled( player_activity &act, Character &who ) {}
virtual std::unique_ptr<activity_actor> clone() const = 0;
virtual void serialize( JsonOut &jsout ) const = 0;
```

注册表（**字符串 id → 工厂函数**）：
```cpp
namespace activity_actors {
extern const std::unordered_map<activity_id,
        std::unique_ptr<activity_actor>( * )( JsonValue & )> deserialize_functions;
}
```

头文件注释原文：派生类「必须创建反序列化的静态成员函数，并加入 `activity_actor_deserializers` 哈希表」。

**新增一种活动 = 新增一个类 + 注册一个反序列化器；核心代码里没有任何 switch(type)。**
→ 我们的 `CreatureDef.component_scripts: Array[Script]` 目前是**裸列表**。应当升级为 **字符串 id → 工厂** 的注册表，数据里写 `"brain": "wander_brain"`，新增大脑不动引擎。

### 1.7 数据层：typed id + generic_factory + copy-from/abstract + 四阶段加载

- **typed id**：`string_id<T>`（JSON 与人读用，存档稳定）↔ `int_id<T>`（内部性能用，是数组下标）。例：`using bodypart_id = int_id<body_part_type>;`、`extern const bodypart_str_id body_part_head;`
- **`generic_factory<T>`**：`load(JsonObject, string)` / `obj(string_id)` / `is_valid()` / `get_all()` / `finalize()` / `reset()`。**只有实现了 generic_factory 的类型才支持 `copy-from`**（文档原文给出的自查法：去 `init.cpp` 找 `add("gate", &gates::load)`，再到 `gates.cpp` 找有没有 `generic_factory gates_data(...)`；`vitamin.cpp` 没有，所以 vitamin 不支持继承）。
- **`copy-from` 继承**：子对象以父对象为初值，只写差异。字段级操作符：`relative`、`proportional`（`0.5` = 50%）、`extend`、`delete`。
- **`abstract`**：只给别的类型继承、自己不能进游戏的模板（加载完即丢弃，缺必填字段也不报错）。
- **四阶段加载**（社区整理的源码结构）：`DynamicDataLoader`（按 `type` 分派、解析）→ `generic_factory::finalize`（**解析 copy-from 链并合并**）→ `finalize_all()`（交叉引用 id）→ `check_all()`（校验所有引用存在、数据合法）。
- **官方告诫**：别滥用 copy-from，「链式继承会难以阅读、等于把冗余换了个地方」；只在两种情况用：①两者几乎相同 ②一组实体**总是**共享某属性集 → 用 1~2 层浅抽象。

**对我们的直接映射**：我们的 `.tres` 就是 JSON 层；但缺三样东西 —— ① `copy_from`（继承父定义）② `abstract`（抽象模板）③ **finalize + check_all 校验**（现在依赖缺失只 `push_warning`）。

### 1.8 其他两个可直接借用的点

- `Creature` 上有个 `int reachable_zone`，注释说明用于划分「共享可达性/可见性」的区域，**用来忽略不同区域里的怪物**（粗粒度空间分区，和雨世界的房间抽象同级）。
- `move_to()` 会调用 `on_move()` 钩子（原文注释：Moves the creature to the given location and calls the `on_move()` handler）。
  → 移动带来的副作用（留脚印、制造噪音、离开领地判定）**挂到钩子上，不用改移动代码**。我们的 `Creature.move_to()` 现在只改坐标和占格，将来加行为会想改它 —— 应该先留钩子。

---

## 2. 雨世界（Rain World）—— 源码闭源，用官方访谈 + 技术档

### 2.1 基类脚本职责（Unity 官方博客，Akupara 开发主管 Andrew / 主程 Joar 原话）

> 「每种生物都继承自基本生物脚本，这些脚本会处理**碰撞**和它们**都需要的其他通用任务**，例如**生命系统、持有物品、保存/加载**它们。」

→ 与我们 `Creature` 基类的定位完全一致（通用状态 + 通用流程）。**这条不用改，是正解。**

### 2.2 模块化 AI：可选挂载、可配置

> 「在行为层面上，生物们使用**模块化的 AI 模块集合**，每个模块都可以被**选择性地连接和配置**，来改变自己可能的行为和优先级。」

官方列出的模块及职责：
| 模块 | 职责 |
|---|---|
| PreyTracker | 猎杀附近猎物 |
| ThreatTracker | 逃离附近捕食者 |
| FriendTracker | 与志同道合者组成合作队伍 |
| ItemTracker | 找地面可拾取物品 |
| DenFinder | 家的感觉，任务后可返回 |
| DiscomfortTracker | 定义该生物通常避开的区域/物体 |

社区技术页补充的 Tracker：`RainTracker`（怕雨）、`InjuryTracker`（自己多伤，蜥蜴有专属 `LizardInjuryTracker`）、`AgressionTracker`（与宿敌的对抗欲）、`NoiseTracker`（听到的声音与探索欲）、`RelationshipTracker`。

→ 这就是我们已经选定的「组合组件」路子。**关键差异**：雨世界把「**同一物种内也可选配**」做得更彻底（模块可配置参数）；我们的组件目前只能靠 `CreatureDef` 决定挂不挂，还不能传参。**组件带参数**是个纯新增能力。

### 2.3 关系是一等公民数据

`RelationshipTracker` 在生物意识到附近有别的生物时触发。关系有**具名类型**，值域 0~1（0 无影响，1 影响最大）：
`Ignores / Eats / Afraid / StayOutofWay / AgressiveRival / Attacks / Uncomfortable / Antagonizes / PlaysWith / SocialDependent / Pack`

且：**关系不是静态的**，随 Reputation（声望，分「区域内声望」和「全局声望」）、Personality 和玩家行为变化。

→ 我们目前**生物之间没有任何关系概念**。而「群居/独行/鱼群效应」本质上就是关系系统。这是纯新增（一个 `Relations` 组件 + 一张关系类型表）。

### 2.4 抽象 / 具现 两级模拟 + 显式性能预算 ★第二重要的发现

技术页（大量标注内部函数名，可交叉验证）：

- 所有房间加载时，除玩家所在房间外**默认是 abstract（抽象）状态**：不模拟物理与碰撞，里面的生物也是抽象的。
- 房间靠近玩家时被 **realize（具现）**，才完整模拟物理、碰撞、生物 AI；离开足够远再 **abstractize（抽象化）**回去。
- 反编译 Modding 文档里的真实函数：`AbstractCreature.Realize()` / `RealizeCustom()` / `Abstractize()`，以及 `abstractAI.RealAI = new XxxAI(...)`；`CreatureTemplate.Type` 枚举 + 构造里 switch 出每种生物的 `XxxState`（`LizardState` / `MouseState` / `HealthState` / `NoHealthState`…）与 `XxxAbstractAI`。
- **预算制**：`RoomRealizer.performanceBudget` 常量 **1500**；每房间有 `RoomPerformanceEstimation()` 公式（含长宽、是否浸水、房间内所有生物的性能开销之和）；**每种生物有固定性能开销**：

| 生物 | 开销 | 生物 | 开销 |
|---|---|---|---|
| Leviathan | 300 | Scavenger | 300 |
| Rain Deer | 200 | Rot | 200 |
| King Vulture / Miros Vulture | 140 | Vulture | 100 |
| Lizard | 50 | Spitter Spider | 50 |
| Garbage Worm | 30 | Big Spider | 30 |
| Jetfish / Squidcada | 25 | Snail / Dropwig | 20 |
| Leech / Batfly | 10 | 其他任何生物 | 10 |

- 抽象化条件举例：房间内无玩家、已完全加载、从未访问或访问超过 5 秒、已具现至少 1 秒（若是下一个可能房间则 10 秒）、且不与聚焦房间直接相连。

> 官方访谈里 Joar 的说法：「**抽象生物框架把生物属性分层**：外观层放美术资源，AI 层放程序逻辑。生物在屏幕外时，把外观层关掉、AI 层留着」——于是**生物永远在存在、永远在过日子**，但不吃满性能。

→ 我们现在是**每只生物每个物理帧都 tick**。这是最需要补的一块，而且它是**纯新增**：加一个「模拟层级」概念 + 一个预算管理器，现有组件一行都不用改（抽象层只喂它更少的 tick）。

### 2.5 性格与社交记忆

- 性格最早实现：**3 个基础值 `sympathy / energy / bravery` → 派生 3 个 `nervousness / aggression / dominance`**。（与社区 wiki 的六维一致，也与我们已核实的六维一致。）
- **Social Memory**（记忆五参数）：`like / fear / temp like / temp fear / know`，追踪「玩家与每个生物」的关系；这就是「动态关系」系统的核心。

→ 印证了我们 v2 方案里「性格 6 维 + 加权融合出行为」的方向；并提示关系记忆应该有 **临时值 / 长期值** 两层（temp like 会衰减，like 稳定）。

---

## 3. 矮人要塞（DF）—— 源码闭源，用 raw 文档 + DFHack 镜像 + 访谈

### 3.1 身体 = 具名可复用模板的**组合** ★第三重要的发现

DF wiki「Body token」页原文：

> 「A creature uses the `[BODY]` creature token to list all of the body templates it includes. Each part listed in each template is then included in the creature.（**生物列出它由哪些身体模板组成，每个模板包含一组部位**）」

组合示例（wiki 原文）：
```
[BODY:QUADRUPED_NECK:SPIKE_LAYOUT_8:SPIKE_BASE_THICK:SPIKE_2]
```
→ 一头四足+8 个粗刺基座+每个基座 2 根刺的生物，**全是拼装出来的**，没有一行代码。

部位定义与连接（wiki 原文 token）：
```
[BP:ID:名字:复数]  [CONTYPE:UPPERBODY|LOWERBODY|HEAD|GRASP|STANCE]
[CON:某个部位]     [CON_CAT:某个CATEGORY]
[CATEGORY:xxx]  [EMBEDDED]  [BREATHE]  [APERTURE]  [CIRCULATION]
[CONNECTOR]  [MOUTH]  [DEFAULT_RELSIZE:80]  [NUMBER:n]
```
另有 **bodygloss**：可以给部位**改名**，从而「复用既有模板而不必为了改名再定义一个几乎相同的模板」。

→ 我们现在是每只猪在自己的 `.tres` 里手写 6 个部位。DF 的做法是：`QUADRUPED_NECK` 这种**具名身体模板**被猪/狼/鹿共同引用。**加一只狼 = 引用同一个模板**，不是复制 6 个 `.tres`。

### 3.2 用 detail plan 把组织层次也数据化

```
[BODY_DETAIL_PLAN:标识符]
  [ADD_TISSUE:标识符:组织模板]      [ADD_MATERIAL:标识符:材料模板]
  [BP_LAYERS:BY_CATEGORY:ALL:SKIN:厚度:...]
  [BP_LAYERS_OVER:...]  [BP_LAYERS_UNDER:...]
  [BP_POSITION:BY_CATEGORY:部位:FRONT|BACK|LEFT|RIGHT|TOP|BOTTOM]
  [BP_RELATION:...:AROUND|SURROUNDED_BY|ABOVE|BELOW|IN_FRONT|BEHIND|CLEANS|CLEANED_BY:父部位:覆盖率]
  [BP_RELSIZE:BY_CATEGORY:部位:相对大小]
```
要点：detail plan 是**可复用的具名集合**，且支持**调用时传变量**（`ARG#`）填空；`BY_CATEGORY / BY_TYPE / BY_TOKEN` 三种**选择器**批量指定部位。

→ 「选择器（按类别/按类型批量选中一组部位）」这个思路对我们很有用：将来给「所有腿」加护甲/加伤，不用逐个点名。

### 3.3 creature variation = 文本级 token 继承（DF 的 mixin）

```
[CREATURE_VARIATION:标识符]
  [CV_NEW_TAG:BIOME:ANY_FOREST]        [CV_ADD_TAG:...]（别名）
  [CV_REMOVE_TAG:BODY:HUMANOID_SIMPLE] （按 token 前缀整体移除）
  [CV_CONVERT_TAG] [CVCT_MASTER:BODY] [CVCT_TARGET:2EYES] [CVCT_REPLACEMENT:2EYESTALKS]
  [CV_NEW_CTAG:2:HUMANOID:CAN_SPEAK]   （条件变体：仅当第 2 个参数是 HUMANOID 才加）
```
应用方式：`[APPLY_CREATURE_VARIATION:ID:arg1:arg2...]` 或 `[APPLY_CURRENT_CREATURE_VARIATION]`；配合 `[COPY_TAGS_FROM]` 就能从**既有生物**派生新生物。
应用顺序（wiki 原文）：**先执行所有 remove（自下而上）→ 再 convert（自下而上，"反序"）→ 最后 add（自上而下）**；`!ARGn` 会被调用参数替换，`|` 在插入时变成 `:`。不能嵌套（`[APPLY_CREATURE_VARIATION]` 本身不能被 variation 添加）。

**评价**：这比 CDDA 的 `copy-from` 更强大（能改任意 token），但是**基于文本、无结构校验**，容易写出难维护的链。
→ 我们选 **CDDA 路线**（结构化字段 + 校验 pass），不选文本 token 手术。

### 3.4 惰性健康结构 + 死亡标志（DFHack 镜像）

```xml
<bitfield-type type-name='unit_flags3'>
  <flag-bit name='compute_health' original-name='WOUND_CHECK_REQUIRED'
            comment='causes the health structure to be created or updated'/>
```
→ **健康结构是按需才创建/更新的**（不是每帧全量维护）——和雨世界「抽象层不模拟」是同一个思想。

```xml
<bitfield-type type-name='unit_flags1'>
  <flag-bit name='inactive' original-name='DEAD'
            comment='Set for dead units and incoming/leaving critters that are alive but off-map'/>
</bitfield-type>
<bitfield-type type-name='unit_flags2'>
  <flag-bit name='killed' original-name='HAS_BEEN_KILLED'
            comment='Has been killed by kill function (slightly different from dead, not necessarily violent death)'/>
</bitfield-type>
```
→ DF 的 `dead` 位**同时用于「死了」和「活着但离开地图」**（离场即置位），且**另设一个 `killed` 位**区分「被杀死」与「以其他方式进入死/离场状态」。这是个值得注意的细节：**「不在场」和「已死」共用同一个廉价标志位**，正好呼应雨世界的抽象层。

### 3.5 Tarn Adams 自己的代码观（2021 StackOverflow 访谈，原文）

- 「DF 是 C 和 C++ 的某种组合，**不是遵循什么标准的方式，而是一坨随时间堆积起来的东西**。」
- 711,000 行；**没有做过正式重构**。「大部分改动只集中在游戏的某些部分，所以有一个我相当熟悉的『活跃熔核』，还有些 2006 年首发前就再没看过的硬壳。」
- 「我尽量把变量和对象**命名得一致、好记**，并留足够的注释。」
- 身体部位与创伤系统**灵感来自 1990 年的桌游《Cyberpunk 2020》**。
- 他「**偏好对单个元素建模，而不是对整个系统建模**」。
- 加 Z 轴是做过最难的事（要改几千处依赖 (x,y) 的逻辑与调用）。

**这条最有价值的其实不是技术，而是取舍**：DF 的世界级复杂度**不是来自优雅架构**，而是来自「数据驱动 + 单元素建模 + 长期不重构但改动局部化」。它印证了用户的核心诉求：**优先在现有结构上增加功能**。

---

## 4. 三者共同的三条硬规律

1. **内容全是数据，代码只提供机制。**
   CDDA 的 JSON + `generic_factory`；DF 的 raw token；雨世界的 `CreatureTemplate` + AI 模块表。三家的「加一个新生物」都不需要改引擎核心。
2. **可扩展点 = 字符串 id → 工厂/注册表，核心代码不做类型分支。**
   CDDA `deserialize_functions` 注册表是范本；DF 靠 `[APPLY_CREATURE_VARIATION]`；雨世界靠「模块可选挂载」。
3. **性能靠「分层/降级模拟」，不靠优化每只生物的每帧。**
   雨世界抽象↔具现 + 显式预算（1500）与逐物种开销；DF 惰性健康结构 + `dead/inactive` 位兼表「离场」；CDDA 的 `reachable_zone` 粗粒度分区。

---

## 5. 对照我们的代码：哪些已经对了、哪些要改

### 5.1 已经对了 —— 不要动

| 我们的做法 | 三家的印证 |
|---|---|
| `Creature` 基类只放通用状态 + tick + 占格 | 雨世界基类脚本「碰撞 + 生命 + 持物 + 存读档」 |
| 能力靠 `CreatureComponent` 组合、`requires()` 声明依赖 | 雨世界「模块化 AI 模块，可选挂载」 |
| 定义来自 `Resource`（`.tres`）而非硬编码 | CDDA JSON / DF raw |
| 系统间用 `EventBus` 信号解耦 | 三家一致的解耦方向 |
| 时钟从 `TimeSystem` 统一来（暂停/倍速白送） | 这是我们要的，三家无冲突 |
| `Creature.die()` 作为唯一死入口 | CDDA 的 `die()` 也是唯一执行入口 ✅ |

### 5.2 需要修正（含我上一版的问题）

| # | 问题 | 依据 | 改法 |
|---|---|---|---|
| 1 | **死亡判定在组件里**（`Body._check_vitals` 自己判自己杀） | CDDA：判定 `is_dead_state()` 在宿主、接线 `check_dead_state()` 在基类 | 加 `Creature.is_dead_state()`（向组件问「我算死吗」）+ `check_dead_state()`（基类接线，tick 末尾调一次）。`Body` 只暴露 `is_vital_down()` |
| 2 | **要害/肢体是硬编码字符串比较**（`category=="head"｜"torso"｜"leg"`） | CDDA：`is_vital` / `is_limb` 是**数据字段** | 给 `BodyPartDef` 加 `is_vital: bool`、`is_limb: bool`；逻辑改读布尔，不再比字符串 |
| 3 | **身体部位是每物种手写扁平表** | DF：`[BODY:模板A:模板B:...]` 具名模板可复用可组合 | 新增 `BodyPlanDef`（具名身体模板）资源；`CreatureDef` 引用若干 plan；`Body` 合并实例化。猪/狼/鹿共享 `QUADRUPED` |
| 4 | **部位无连接关系** | CDDA `main_part/connected_to/opposite_part`；DF `CON/CONTYPE/CON_CAT` | `BodyPartDef` 加 `connected_to`（+可选 `opposite_part`、`limbtypes` 加权） |
| 5 | **组件是裸列表，无法按 id 引用/注册** | CDDA `activity_actor` 注册表 | `CreatureDef` 组件改为**字符串 id 列表** + 新增 `Registry` autoload（id → Script）；新增组件不改引擎 |
| 6 | **缺最终校验 pass**，依赖缺失只 `push_warning` | CDDA 四阶段：finalize 合并 → finalize_all 交叉引用 → **check_all 校验** | 加一个启动期 `finalize_all()` + `check_all()`，把所有 `def` 引用校验一遍并**报错** |
| 7 | **组件不能带参数** | 雨世界模块「可选挂载**并可配置**」 | 允许 `CreatureDef` 里覆写组件参数（如 `wander.step_interval`） |

> 说明：以上 7 条**都是「新增/数据补齐」**，除第 1、2 条要动到既有逻辑的**读取方式**（不改变行为语义），其余不触碰现有代码路径。

### 5.3 可以纯新增的能力（按研究发现的优先级）

| 能力 | 来源 | 形态 |
|---|---|---|
| **模拟层级（具现/抽象）** | 雨世界 §2.4 | 新组件/标签 `SimulationLevel` + `SimulationBudget` 管理器（每物种开销、按距离决定具现） |
| **状态效果系统** | CDDA §1.4 | `Effects` 组件 + `EffectDef` 资源；二维键（效果 × 部位），带 duration/intensity/permanent |
| **关系与社交记忆** | 雨世界 §2.3/2.5 | `Relations` 组件 + 关系类型表；五参数记忆 `like/fear/temp_like/temp_fear/know`（临时值会衰减） |
| **移动钩子 `on_move()`** | CDDA §1.8 | `Creature.move_to()` 末尾发 `EventBus.creature_moved(creature, from, to)`；之后所有「移动副作用」挂这里 |
| **出血写进世界** | CDDA §1.5 | 新增「血渍」层（格子级），出血时往当前格加；不改掉血逻辑 |
| **定义继承 `copy_from` + `abstract`** | CDDA §1.7 | `CreatureDef` 加 `copy_from` / `abstract`；加一个 finalize 合并器。狼 = 一个继承犬科的 `.tres` |
| **身体模板可复用 + 选择器** | DF §3.1/3.2 | `BodyPlanDef` + 「按类别批量选部位」（给所有腿加护甲不打名） |
| **惰性身体/健康结构** | DF §3.4 | 抽象层生物不实例化 `Body`/`Needs` 的完整结构，进场才建 |

**明确不做**：DF 式「文本 token 手术」继承（无结构校验、易成硬壳）。

---

## 6. 落地顺序（建议，待拍板）

按「依赖少、风险低、为后面铺路」排序：

1. **数据化要害/肢体 + `is_dead_state()`/`check_dead_state()`**（修正 1、2）
   最小、纯收益，且是其他一切的基础（后面加 Effect、BodyPlan 都要靠数据化标志）。
2. **定义校验 pass（finalize + check_all）**（修正 6）
   在继续堆数据之前先把「引用错了会报错」建起来，否则后面幻觉式排错。
3. **`Registry`（id → Script）+ 组件带参数**（修正 5、7）
   开放式扩展的地基；也是升级方案里 `Personality`、`Perception`、`Relations` 等新组件的落地前提。
4. **`BodyPlanDef` 具名身体模板**（修正 3、4）
   加狼/鹿不再复制部位表。
5. **`on_move()` 钩子**（5.3）
   为「留痕/噪音/领地」预备，成本极低。
6. **`SimulationLevel` + `SimulationBudget`**（5.3）
   规模上来前必须做，现在做也不亏。
7. 之后才是：状态效果系统 → 关系/社交记忆 → 性格驱动行为（v2 方案）。

---

## 附：主要来源

- CDDA 源码：`github.com/CleverRaven/Cataclysm-DDA` → `src/creature.h` `src/creature.cpp` `src/bodypart.h` `src/effect.h` `src/activity_actor.h`；`doc/JSON_INHERITANCE.md`
- 雨世界：Unity 官方博客《Exploring procedural design in Rain World: The Watcher》（Akupara 开发主管原话）；`rainworld.miraheze.org` → `Technical Glossary/Abstractization`、`Remix/Rain World Remix`、`Behavior`；`rain-world-modding.fandom.com` → `Adding a Custom Creature`（反编译函数名）
- 矮人要塞：`dwarffortresswiki.org` → `Body token`、`Body detail plan token`、`Creature variation token`；`github.com/DFHack/df-structures` → `df.unit.xml`；Tarn Adams 访谈（StackOverflow Blog, 2021-07-28）
