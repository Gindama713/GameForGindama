# 生物大类（Creature）基础属性思考

> 目标：定义「所有生物共用的一套底层逻辑」应包含哪些属性。
> 参考：雨世界（Rain World）的真实机制 + 你的荒岛求生语境。
> 纪律：数值全标「待定」，不编造；来源已注明。

---

## 0. 雨世界是怎么做的（已核实，非编造）

来自 Akupara / Videocult 的开发者访谈（Unity 官方博客）：

> *"Every creature inherits from base Creature scripts, which handle collisions and other general tasks they all need, such as health systems, held items, and saving/loading."*
> —— 每只生物都继承同一套 base Creature 脚本，统一处理碰撞、健康系统、持物、存读档。

行为层用**可选挂接的模块化 AI**（原文列举）：
- `PreyTracker`（追猎附近猎物）
- `ThreatTracker`（躲避附近捕食者）
- `FriendTracker`（与同类组队）
- `ItemTracker`（找地上的可用物去捡/拿）
- `DenFinder`（回巢/回家）
- `DiscomfortTracker`（避开让它不适的区域/物体）

蛞蝓猫（玩家）的基础属性（已核实）：`Lung Capacity`（肺活量/水下憋气）、`Throwing Skill`、`Spear Damage`、`Run Speed`、`Mass`（质量）、`Loudness` 等。

状态效果（已核实，全生物通用）：`Starving`(饥饿)、`Stunned`(击晕)、`Tranquilized`(麻痹/镇定)、`Poisoned`(中毒)、`Restrained`(束缚)、`Sedated`(安抚)、`Slowed`(减速)、`Hypothermic`(失温，Saint 线)。

**关键结论**：雨世界的「统一」指的是**引擎/系统统一**，不是把所有属性硬塞进一个大类。物种差异 = 同一套底座上，挂不同的需求系统 + 不同的 AI 模块 + 不同的数值。

---

## 1. 给我们的 Creature 大类分层（建议）

把属性按「性质」分层，而不是直接塞成几十个变量。这样「底层逻辑一样」= 同一套层结构；「差异」= 每层里哪些被激活、数值多少。

### A 层 · 代谢需求（实时属性，随 TimeSystem 时钟 tick）
这些是你说的「实时运算的属性」。雨世界有 `food` / `breath` / `warmth`；荒岛语境补上 `thirst`。

| 属性 | 来源 | 含义 |
|---|---|---|
| `food` 饥饿/能量 | 雨世界 food pips | 空了进入 starving（减速、易晕）；靠吃补充；睡眠/休眠结算 |
| `thirst` 口渴 | **非雨世界，CDDA/荒岛补充** | 雨世界没有口渴，但荒岛求生必须有；空了掉血/掉意识 |
| `warmth` 体温 | 雨世界 hypothermia | 低了失温掉血；火/遮蔽/白天回升；夜/水/风下降 |
| `fatigue` 疲劳 | 雨世界 exhaustion/slowed | 高到阈值→强制减速、易晕；休息/睡恢复 |
| `oxygen` 氧气/呼吸 | 雨世界 Lung Capacity | 水下/窒息时下降，空了开始淹/窒息掉意识 |

> **重要设计点**：不是每只生物都真正需要这 5 个。鱼不需要 `thirst`，螃蟹不太受 `warmth` 影响。**所以这些不该是 Creature 硬编码字段，而应该是「该物种激活了哪些需求系统」的数据**。Creature 提供统一的 Need 框架，物种数据挑激活哪几个、速率多少。

### B 层 · 生命与身体（伤害与死亡）
对应你之前 node 树里的 `Metabolism` + `Body` 子系统。

| 属性/结构 | 来源 | 含义 |
|---|---|---|
| `blood` 总血量 **或** 部位 hp 聚合 | 待定（你之前要拍板） | 死亡阈值所在层 |
| `consciousness` 意识 | 雨世界 stun 阈值 | 独立维度：血没空也可能被击晕/ incapacitated。0=昏迷（可复苏），不是死亡 |
| `Body.Part[]` 身体部位 | 你之前定的 头/躯干/双臂/双腿 | 每部位独立 hp/感染/出血；断腿→不能动，伤臂→不能制作（涌现式能力） |

> 雨世界实锤细节：`stun` 时长与血量相关——重伤生物被击晕**最多长 67%**；饥饿时击晕再长 50%；饥饿+疲劳叠加长 100%。→ 说明「状态之间互相放大」是底层该支持的，不是特例。

### C 层 · 状态效果（瞬时、可叠加、会衰减）
雨世界整页都是这类。建议做成一个统一 `StatusEffect` 列表，每种是一个对象，带 `intensity` + `decay`，而非硬编码一堆 bool。

| 状态 | 来源 | 行为（已核实） |
|---|---|---|
| `bleeding` 出血 | 通用 | 持续掉血，直到包扎 |
| `infection` 感染 | 通用 | 伤口未处理→持续恶化/掉血/发烧 |
| `poison` 中毒 | 雨世界 | 持续掉血 + 周期性击晕 + 抽搐（原文：health 持续降，50% 概率晕 2~16 帧） |
| `paralysis` 麻痹/束缚 | 雨世界 tranquilized/restrained | 不能行动，可被击晕挣脱 |
| `stun` 击晕 | 雨世界 | 倒地不能动；与新 stun 比较取长者覆盖 |
| `slowed` 减速 | 雨世界 | 速度降最多 50%，非二值、强度随源变化 |
| `hypothermic`/`overheat` 失温/过热 | 雨世界 | 接 A 层 warmth 极端值触发 |

### D 层 · 物理（移动基础）
| 属性 | 来源 | 含义 |
|---|---|---|
| `mass` 质量 | 雨世界 Mass | 影响速度、击晕时长、击退；饥饿时 mass 变小（雨世界实锤） |
| `velocity` 速度/动量 | 通用 | 移动手感；网格游戏里可简化为「每 tick 移动几格」 |

### E 层 · 认知/AI（仅 NPC，模块化）
直接映射雨世界的 6 个 Tracker，换成荒岛语义：

| 雨世界模块 | 荒岛语义 |
|---|---|
| `PreyTracker` | 觅食：找可吃物 |
| `ThreatTracker` | 避害：躲捕食者/危险地形 |
| `FriendTracker` | 群居：与同类协作 |
| `ItemTracker` | 拾物：捡可用素材 |
| `DenFinder` | 巢穴：受伤/夜归窝 |
| `DiscomfortTracker` | 不适：避开过冷/过险/过亮区 |

> **只有非玩家生物挂 E 层。** 玩家的「脑」是输入/UI，不在 Creature 上（呼应你之前那棵节点树里 `Brain` 仅 NPC 挂）。

---

## 2. 落地到我们已有的结构

你之前那棵节点树（Creature 根 → Metabolism / Body / Brain / Inventory / Sprite2D）+ Autoload（TimeSystem / EventBus）正好装得下：

- `Metabolism` 子系统 = A 层（需求）+ B 层 blood/consciousness 的 tick。放 `_physics_process`，靠 `TimeSystem` 推进。
- `Body` 子系统 = B 层部位 + C 层状态效果（状态效果挂在 Body 上，因为都作用在身体上）。
- `Brain`（仅 NPC）= E 层 Trackers。
- `Sprite2D` = 纯表现，可删不影响逻辑（你「表现次之」的调性）。

**最关键的架构纪律（来自雨世界 + 你的「幻觉」担忧）**：
> Creature 基类只定义「框架」，不写死具体需求。具体哪些 Need 激活、数值、部位清单、AI 模块——全部在数据（`CreatureDef` Resource）里。
> 加一只螃蟹 = 配一份 `CreatureDef`（激活 thirst=否、warmth=弱、part=壳+螯），不写新系统代码。

---

## 3. 需要你拍板的（不替你定）

1. **死亡模型**：单独一条 `blood` 血条？还是「部位 hp 聚合，关键部位空=死」？（你之前 plan 里留了这题）
2. **需求系统裁剪**：先全生物激活 `food+thirst+warmth+fatigue+oxygen` 五套（简单但可能不真），还是直接上「物种数据挑激活」（更真但先要多写一层 Need 框架）？建议先后者，省得以后返工。
3. **thirst 要不要**：雨世界没有，但荒岛求生我认为必须有——你认可吗？
4. **第一个做哪个属性**：建议从 `food`（最直观、好验证「饿到 0 会怎样」）或 `consciousness`（独立于血、能演示「击晕≠死」）起步。

---

*待办：以上任何一条定了，就把对应属性写成纯逻辑（print 验证），屏幕继续纯黑。*
